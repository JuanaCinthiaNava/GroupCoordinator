// App route group layout — placeholder for Plan 01-04 to add the sticky 56px header.
// For Phase 01-01, this is a passthrough RSC that renders children.
// TODO(Plan 01-04): replace with the app shell — sticky header (56px), Logo + plan title + member stack + reserved search slot + auth affordance.
export default function AppLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
