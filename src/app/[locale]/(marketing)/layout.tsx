// Marketing route group layout — minimal, no app chrome (no header, no nav).
// Used for `/` and any future public marketing pages.
export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
