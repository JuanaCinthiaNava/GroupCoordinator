---
context: phase
phase: 02-itinerary
task: discuss-complete
total_tasks: 5
status: context_captured_ready_for_planning
last_updated: 2026-05-25T19:22:50.896Z
---

# Resume Point: Phase 2 Discuss Done — Next is UI-Phase or Plan-Phase

Phase 2 (Itinerary) **context is fully captured**. 14 implementation decisions (D-24..D-37) locked in `02-CONTEXT.md`. The next step is either UI-SPEC generation (recommended for a visible surface like the timeline) or plan-phase directly.

## Critical Anti-Patterns

Same as the prior session's handoff (carried forward — none have been retired):

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Executor API socket closure mid-stream | Wave 6 of Phase 1 lost its socket before SUMMARY.md write; commits intact, SUMMARY reconstructed by orchestrator | advisory | If an executor returns without SUMMARY.md, do NOT respawn — verify commits exist on the worktree branch, reconstruct SUMMARY.md from commit messages + diffs, commit on the worktree branch, then merge. |
| Worktree absolute-path drift | Phase 1 Wave 3 executor wrote to the MAIN repo because absolute paths leaked outside the worktree | advisory | Every executor prompt MUST require deriving `WT_ROOT=$(git rev-parse --show-toplevel)` and verifying `pwd` matches before any file op. Required clauses already in the execute-phase prompt template. |
| Executor worktrees lack node_modules | All 6 Phase 1 waves skipped local `pnpm exec tsc / pnpm test:unit`; orchestrator's post-merge gate caught everything | advisory | Continue the orchestrator-runs-the-pnpm-gate-post-merge pattern. Phase 2 plans should note that integration verification happens post-merge in main, not inside the worktree. |
| Auto Mode generic-authorization blocks | "Public" answer from AskUserQuestion did NOT count as explicit per-action auth for `gh repo create --public` | advisory | When a destructive action follows a multiple-choice answer, the action call must include a one-sentence explicit-authorization confirmation citing the resource + scope. |

No `blocking` constraints.

<current_state>
- Phase 1: **complete, verified, pushed**. 5 of 7 verification follow-ups fixed (1, 2, 5, 6, 7); 2 deferred to Phase 7 (atomicity of createPlan, N+1 in getMyPlans).
- Phase 2: **discuss-phase done**. `02-CONTEXT.md` + `02-DISCUSSION-LOG.md` committed (6339e77, 68c8a44). No SPEC.md, no RESEARCH.md, no UI-SPEC.md, no PLAN.md yet.
- Repo: 56 commits on `origin/main`, in sync. Identity: `Cinthia Nava <juana.nava@definityfirst.com>`. GitHub: `JuanaCinthiaNava/GroupCoordinator` (public).
- Build / type-check / 65 unit+integration tests all pass against main. node_modules installed.
- Docker NOT installed; Supabase local NOT running. Phase 2 plan-phase + execute-phase can proceed without it; integration tests will skip-clean.
</current_state>

<completed_work>

**This session (continuing from prior pause):**

- Resumed from prior handoff
- Closed 5 of 7 Phase 1 verification follow-ups:
  - `d3dbae4` — Removed stale TODOs in `PlanSignInSheet.tsx` and `require-user.ts`
  - `76a8898` — `updatePlan` auth-failure redirect target fixed (`/plan` → `/me`)
  - `f0cb28e` — Extracted `safeNext` to `src/lib/auth/safe-redirect.ts` + 6 unit tests
  - `f49f665` — Extracted `SignInSheetBody` shared by `/auth/sign-in` and the inline plan sheet
- Followups 3 (createPlan atomicity) + 4 (getMyPlans N+1) explicitly deferred to Phase 7
- Skipped manual local-validation per user direction (Docker not installed; can be done any time before public launch)
- Captured Phase 2 context (D-24..D-37) and discussion log:
  - `6339e77` — `docs(02): capture phase context`
  - `68c8a44` — `docs(state): record phase 2 context session`
</completed_work>

<remaining_work>

**Phase 2 pipeline:**

1. **`/gsd-ui-phase 2`** — Recommended next. Produces `02-UI-SPEC.md` with the timeline anatomy, day-section header, item card, category badge style, and the create/edit form Surface. Aligns with Phase 1 visual rhythm (emerald-700 accent, Geist, 4-pt grid).
2. **`/gsd-plan-phase 2`** — Decomposes into 4–6 plans (estimate based on Phase 1 having 6). Likely shape:
   - 02-01 — schema migration (`itinerary_items` table + `itinerary_category` enum + `plans.timezone` column + RLS policies)
   - 02-02 — Server Actions (createItem / updateItem / deleteItem) + rule-based categorize helper + validation Zod schemas
   - 02-03 — Timeline RSC (`ItineraryList`, day grouping, item card render, attribution line, category badge)
   - 02-04 — Create / edit item form (Sheet on mobile, Dialog on desktop, date+time picker)
   - 02-05 — Plan timezone setter UI in Settings page (extend `PlanDetailsForm` or new section)
   - 02-06 — Tests + microcopy + verification
3. **`/gsd-execute-phase 2`** — Wave-based execution with worktree isolation, mirroring Phase 1's pattern.
4. **Verify** — Either automatic at end of execute-phase or via `/gsd-verify-work 2`.

**Phase 1 manual checkpoints still open** (non-blocking, can be exercised any time):
- Google OAuth real round-trip in a browser
- WhatsApp OG preview test
- Live-DB integration suite via `pnpm supabase start` (needs Docker)
- webkit-mobile visual pass
- Manual revoke-then-paste flow

</remaining_work>

<decisions_made>

**14 Phase 2 decisions captured in `02-CONTEXT.md`:**

| ID | Topic | Decision |
|----|-------|----------|
| D-24 | Item timing | `scheduled_at TIMESTAMPTZ NOT NULL` — date+time always required |
| D-25 | Notes format | Plain text + URL auto-link, max 2000 chars; no markdown |
| D-26 | Category | enum `transport/food/activity/other` NULL allowed; rule-based keyword matcher es/en/pt, server-side on create/update |
| D-27 | Item columns | id, plan_id FK CASCADE, title 200, scheduled_at, place_text, notes, category, created_by, timestamps |
| D-28 | Permission model | Wiki-open: editor + owner CRUD any item; viewer read-only |
| D-29 | RLS policies | 4 policies on itinerary_items (select anon-or-member; insert/update/delete editor-or-owner) |
| D-30 | Delete model | Hard delete (no deleted_at) — diverges from Phase 1 intentionally |
| D-31 | Attribution | "Agregado por {nombre}" via created_by; no last_edited_by |
| D-32 | Place coupling | Phase 2 ships `place_text NULL`; Phase 3 adds `place_id` sibling — no destructive migration |
| D-33 | Day grouping | Only days with items render; no "Sin actividades" placeholders |
| D-34 | Time zone | Plan-level TZ via `plans.timezone TEXT NOT NULL`; default detected from browser at plan create |
| D-35 | Out-of-range items | Allowed; linear timeline, no "before/after trip" section |
| D-36 | Fetch pattern | RSC + Server Actions + `revalidatePath` (Phase 1 pattern); no TanStack Query, no Realtime |
| D-37 | i18n | New namespace `itinerary.*` in es/en/pt JSON; Biome enforces no-hardcoded-strings |

**Schema additions queued for the planner** (single migration `0003_itinerary.sql` or split):
- `ALTER TABLE plans ADD COLUMN timezone TEXT NOT NULL DEFAULT 'America/Mexico_City'`
- `CREATE TYPE itinerary_category AS ENUM (...)`
- `CREATE TABLE itinerary_items (...)`

**Style bias signaled by the user** (12/12 sub-questions chose "Recommended" simplest option):
- Honor MVP-first scope
- Do NOT speculatively add `category_confidence`, `last_edited_by`, `duration_minutes`, `deleted_at`, TanStack Query, Realtime
- The wiki-open + hard-delete simplicity is intentional, not lazy
</decisions_made>

<blockers>

None.
</blockers>

## Required Reading (in order)

1. `.planning/phases/02-itinerary/02-CONTEXT.md` — full text of the 14 decisions + 11 deferred ideas + canonical refs
2. `.planning/phases/02-itinerary/02-DISCUSSION-LOG.md` — the 12 sub-question alternatives considered (for retrospective)
3. `.planning/REQUIREMENTS.md` — ITIN-01..05 acceptance criteria (locked, do NOT renegotiate)
4. `.planning/phases/01-spine-plan-lifecycle/01-CONTEXT.md` — Phase 1 D-01..D-23 invariants that flow forward (RLS, Drizzle, Server Actions, shadcn, next-intl, role enum, anon JWT)
5. `.planning/phases/01-spine-plan-lifecycle/01-VERIFICATION.md` — Phase 1's audit + the 2 still-open follow-ups Phase 7 will handle
6. `.planning/phases/01-spine-plan-lifecycle/01-02-SUMMARY.md` — authoritative for the existing `plans`/`plan_members`/`invite_tokens` shapes Phase 2 builds on
7. `.planning/PROJECT.md` — "wiki colaborativo" framing for the permissions decision; wedge headline (findability)

## Critical Anti-Patterns (do NOT repeat these)

- **Respawn-on-API-error** — if the Phase 2 executor's socket dies mid-wave, reconstruct SUMMARY.md from commits, don't respawn
- **Worktree absolute-path drift** — every executor prompt MUST derive `WT_ROOT` and verify `pwd`
- **Skipping post-merge pnpm gate** — orchestrator runs `pnpm install --prefer-offline && pnpm exec tsc --noEmit && pnpm test:unit && pnpm build` in main checkout after every wave
- **Wiring TanStack Query speculatively** — Phase 2 explicitly defers it; planner should NOT introduce it

## Infrastructure State

- **Git remote**: `origin → https://github.com/JuanaCinthiaNava/GroupCoordinator.git` (public)
- **Branch**: `main` tracking `origin/main`, in sync
- **Local git identity**: `Cinthia Nava <juana.nava@definityfirst.com>` (local to this repo)
- **gh CLI**: `~/.local/bin/gh` v2.92.0; credential helper writes to `~/.gitconfig`
- **gh auth**: `JuanaCinthiaNava` (keyring, scopes: gist read:org repo)
- **pnpm**: v9.15.9 via nvm node v24.15.0; node_modules installed and up-to-date
- **Docker**: NOT installed — Supabase local stack unavailable; integration tests skip-clean
- **No active worktrees**, **no background processes**

<context>
Two sessions of intensive work behind us. Phase 1 is whole. Phase 2 has its context — 14 decisions that lean MVP, with a clean handoff to Phase 3 baked in (place_text dual-column). Phase 2's planning surface is small enough that one ui-phase + one plan-phase + ~4–6 plans should suffice.

The user has been picking the simpler option repeatedly. Planner should honor that — Phase 2 must NOT become "a giant feature with rich attribution + realtime + AI categorization". Ship the minimum that satisfies ITIN-01..05, with category auto-detect via keyword matcher (the one non-default choice the user made), and stop there.

Phase 3 (Map & Places) is the natural follow-up. Phase 2's place_text column is intentionally a stub that Phase 3 will sibling-extend, not replace. Phase 3 will own its own discuss-phase decisions about geocoding strategy, the "promote text → place" UX, and MapLibre integration.
</context>

<next_action>
Recommended: `/gsd-ui-phase 2` to produce `02-UI-SPEC.md` for the timeline surface (day-section header, item card anatomy, category badge style, create/edit form layout). This locks the visual contract before planning, mirroring Phase 1's successful sequence.

Alternative: `/gsd-plan-phase 2` directly — the planner will make UI calls inline but without a UI-SPEC contract to verify against.
</next_action>
