---
phase: 01-spine-plan-lifecycle
plan: 06
type: execute
wave: 6
depends_on:
  - 01-05
files_modified:
  - src/app/[locale]/(app)/plan/[slug]/settings/page.tsx
  - src/components/plan/settings/PlanDetailsForm.tsx
  - src/components/plan/settings/InviteTokensSection.tsx
  - src/components/plan/settings/TokenRow.tsx
  - src/components/plan/settings/InlineConfirm.tsx
  - src/components/plan/settings/PlanStatusSection.tsx
  - src/components/plan/settings/ArchiveDialog.tsx
  - src/server/actions/plan.ts
  - src/server/actions/invite-token.ts
  - src/lib/validation/plan.ts
  - tests/integration/revoke-token.test.ts
  - tests/integration/archive-plan.test.ts
  - tests/e2e/token-revoke.spec.ts
  - tests/e2e/archive-plan.spec.ts
autonomous: false
requirements:
  - PLAN-04
  - PLAN-05
  - AUTH-06
truths:
  - "/plan/[slug]/settings is owner-only; non-owner authenticated user gets redirected to /plan/[slug] (UI-SPEC §Surface 6 owner gate)"
  - "Anonymous user visiting /plan/[slug]/settings gets redirected to /auth/sign-in?next=/plan/[slug]/settings (AUTH-06)"
  - "PlanDetailsForm allows editing title, start_date, end_date, description; updatePlan Server Action validates via Zod and applies via RLS-bound supabase client (plans_update_owner_only enforces)"
  - "Token row UI shows: editable name (defaults to 'Link {N}' from es.json template), 'Creado: {date} · Usado {N} veces', and 'Revocar link' destructive action; clicking expands inline confirm (no modal — Gmail-style per UI-SPEC §Modal/Sheet Base Rules)"
  - "revokeInviteToken Server Action sets revoked_at = now() via RLS-bound client (invite_tokens_update_owner enforces); after revoke, fresh paste of /i/[old-token] in incognito routes to /errors/token-revoked (PLAN-04)"
  - "Generate new link CTA mints a fresh token (mintInviteToken from Plan 01-04) and the new row appears at the top of the list with name 'Link {N+1}'"
  - "Archive flow: 'Archivar plan' opens Dialog with copy 'archive_dialog_*' keys; archivePlan Server Action sets archived_at = now() via RLS-bound client; archived plan disappears from /me (filtered by archived_at IS NULL in Plan 01-05's getMyPlans)"
  - "Delete button in UI maps to archive behavior (RESEARCH §Open Question 5 — soft-delete only; Eliminar = Archivar with stronger confirmation copy)"
  - "Token revoke + plan archive are SOFT operations only; no DELETE policies exist on invite_tokens or plans (Plan 01-02 enforced); audit trail preserved (D-05)"
must_haves:
  truths:
    - "Plan owner can revoke an invite token; the revoked /i/[token] route returns the token-revoked error page (PLAN-04, ROADMAP success criterion 4)"
    - "Plan owner can archive a plan; the plan disappears from /me but remains accessible by direct URL (data preserved; soft-delete) (PLAN-05)"
    - "All operations use RLS-bound client (no service-role bypass) — Plan 01-02 policies are the source of truth"
  artifacts:
    - path: src/app/[locale]/(app)/plan/[slug]/settings/page.tsx
      provides: "Surface 6 RSC; owner gate; renders PlanDetailsForm + InviteTokensSection + PlanStatusSection"
    - path: src/components/plan/settings/TokenRow.tsx
      provides: "Each token row — editable name + meta + revoke inline-confirm; UI-SPEC §Surface 6 token rows"
    - path: src/components/plan/settings/InlineConfirm.tsx
      provides: "Reusable Gmail-style inline expand-to-confirm for destructive actions in lists"
    - path: src/components/plan/settings/ArchiveDialog.tsx
      provides: "Dialog wrapping archive + delete confirmation; uses `plan.settings.archive_dialog_*` and `plan.settings.delete_dialog_*` keys"
    - path: src/server/actions/plan.ts (updated)
      provides: "updatePlan + archivePlan now implemented (Plan 01-04 stubs replaced)"
    - path: src/server/actions/invite-token.ts (updated)
      provides: "revokeInviteToken now implemented; renameInviteToken added"
  key_links:
    - from: src/app/[locale]/(app)/plan/[slug]/settings/page.tsx
      to: src/lib/auth/require-user.ts
      via: getRequiredUser + owner check via `plan.owner_id === user.id`
      pattern: "require-user|owner_id"
    - from: src/server/actions/plan.ts
      to: src/lib/validation/plan.ts
      via: updatePlanSchema, archivePlanSchema
      pattern: "updatePlanSchema|archivePlanSchema"
    - from: src/server/actions/invite-token.ts
      to: src/lib/supabase/server.ts
      via: RLS-bound client for invite_tokens UPDATE (invite_tokens_update_owner policy)
      pattern: "createServerClient"
---

<objective>
Ship the plan-owner controls: edit details, mint/rename/revoke invite tokens, archive (= delete UX) plans. Closes PLAN-04 + PLAN-05, completes the Phase 1 ROADMAP success criteria, and finalizes the Walking Skeleton's owner side.

Purpose: After this plan ships, Phase 1 is verification-ready. The Walking Skeleton owner can do the full create → share → manage tokens → archive lifecycle. Every Phase 1 requirement (AUTH-01..06 minus AUTH-03 deferred, PLAN-01..06) is covered. Gives CP-5 abandoned-plan recovery a foothold (archive ≠ delete; data preserved).

Output: One settings page (Surface 6) + 6 settings components + 2 Server Action updates + 2 integration tests + 2 un-skipped E2E specs.

User story (MVP_MODE): **As a** plan owner, **I want to** revoke a leaked invite link or archive a plan that's run its course, **so that** I control who can join my plan and keep my "Mis planes" list clean without losing the data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-spine-plan-lifecycle/01-CONTEXT.md
@.planning/phases/01-spine-plan-lifecycle/01-RESEARCH.md
@.planning/phases/01-spine-plan-lifecycle/01-UI-SPEC.md
@.planning/phases/01-spine-plan-lifecycle/01-VALIDATION.md
@.planning/phases/01-spine-plan-lifecycle/01-SKELETON.md
@.planning/phases/01-spine-plan-lifecycle/01-04-SUMMARY.md
@.planning/phases/01-spine-plan-lifecycle/01-05-SUMMARY.md

<interfaces>
<!-- Surface 6 contract from UI-SPEC §Surface 6 — Plan Settings: -->

Owner gate: redirect to /plan/[slug] if user.id !== plan.owner_id; redirect to /auth/sign-in?next=… if not authenticated.

Anatomy (Surface 6):
- Header: sticky, back chevron (lucide ChevronLeft) + "Configuración" title (key plan.settings.page_title)
- Sections (space-y-6):
  1. "Detalles del plan" — edit title/dates/description (same field specs as Surface 1; reuse CreatePlanForm shape or build PlanDetailsForm); 'Guardar cambios' button (key common.save)
  2. "Links de invitación" — section header 'plan.settings.invites_active' (count interpolated); 'plan.settings.generate_link' CTA (outline button full-width with Plus icon); token row list space-y-3
  3. "Estado del plan" — 'plan.settings.archive_button' (destructive outline full-width); 'plan.settings.delete_button' (destructive ghost, smaller, zinc-500; behavior IS the same as archive — soft-delete with stronger copy per RESEARCH §Open Question 5)

Token row (each):
- Row card p-4 rounded-lg shadow-sm
- Editable name (defaults to "Link {N}"; tap to edit inline, blur or Enter confirms) — text-body semibold
- Meta line: text-small (14px) zinc-500: 'plan.settings.token_created' + ' · ' + 'plan.settings.token_used'
- "Revocar link" destructive text action (text-small red-600)
- Inline confirm (expanded state): copy 'plan.settings.token_revoke_confirm_copy', confirm button 'plan.settings.token_revoke_confirm_action' (red filled), cancel ghost — no modal, no toast
- After revoke: row removes from list with 200ms fade-out

Archive dialog:
- Title: 'plan.settings.archive_dialog_title'
- Copy: 'plan.settings.archive_dialog_copy'
- Confirm button: 'plan.settings.archive_confirm' (destructive red filled)
- Cancel button: 'common.cancel'
- Focus on Cancel (safe default per UI-SPEC §Modal/Sheet Base Rules)

Delete dialog (same UI behavior, stronger copy):
- Title: 'plan.settings.delete_dialog_title' ("¿Eliminar este plan?")
- Copy: 'plan.settings.delete_dialog_copy' ("Esta acción archivará el plan. Puedes recuperarlo contactándonos.")
- Confirm button: 'plan.settings.delete_confirm'
- Cancel: 'common.cancel'

From Plans 01-02 → 01-05:
- src/lib/validation/plan.ts — createPlanSchema; this plan adds updatePlanSchema + archivePlanSchema + revokeTokenSchema + renameTokenSchema
- src/server/actions/plan.ts — has createPlan; this plan implements updatePlan + archivePlan (Plan 01-04 stubs)
- src/server/actions/invite-token.ts — has mintInviteToken; this plan implements revokeInviteToken + renameInviteToken
- Plan 01-02 RLS policies: plans_update_owner_only, invite_tokens_update_owner (UPDATE-only, no DELETE)
- Plan 01-05 PlanHeader exposes "Configuración del plan" menu item for owners

es.json keys consumed (all present from Plan 01-01):
- plan.settings.page_title, section_details, section_invites, invites_active, generate_link, token_created, token_used, token_default_name, token_revoke, token_revoke_confirm_copy, token_revoke_confirm_action
- plan.settings.section_status, archive_button, archive_dialog_title, archive_dialog_copy, archive_confirm, delete_button, delete_dialog_title, delete_dialog_copy, delete_confirm
- common.cancel, common.save
- nav.back

Token naming: Plan 01-04 mints tokens without names. This plan adds a `name` column to invite_tokens OR uses display-time computation: defaulting to "Link {position}" where position is the 1-based order_by_created_at_asc index. Decision: ADD a nullable `name TEXT` column to invite_tokens via a Drizzle migration; if null, display "Link {N}". Document the migration in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration for token name + Server Actions (update, archive, revoke, rename) + integration tests</name>
  <files>drizzle/schema.ts, drizzle/migrations/0002_invite_token_name.sql, src/lib/validation/plan.ts, src/server/actions/plan.ts, src/server/actions/invite-token.ts, tests/integration/revoke-token.test.ts, tests/integration/archive-plan.test.ts</files>
  <read_first>
    - .planning/phases/01-spine-plan-lifecycle/01-RESEARCH.md §Open Question 5 (soft-delete only — Eliminar = Archivar), §Open Question 2 (use_count atomic note — not changed here)
    - .planning/phases/01-spine-plan-lifecycle/01-CONTEXT.md (D-04 revoke semantics, D-05 soft-delete via archived_at)
    - drizzle/schema.ts (current shape from Plan 01-02)
    - supabase/policies/invite_tokens.sql, supabase/policies/plans.sql (Plan 01-02 — update policies are the only mutation surface)
    - src/lib/validation/plan.ts (Plan 01-02 — Zod schemas to extend)
    - src/server/actions/plan.ts, src/server/actions/invite-token.ts (Plans 01-04 stubs to implement)
  </read_first>
  <action>
    1) Add `name: text('name')` (nullable) to `invite_tokens` in `drizzle/schema.ts`. Run `pnpm drizzle-kit generate` — it will emit `drizzle/migrations/0002_invite_token_name.sql` with `ALTER TABLE invite_tokens ADD COLUMN name TEXT;`. Apply: `pnpm drizzle-kit migrate`. No new RLS policy needed (the existing invite_tokens_update_owner covers all column updates by the owner).

    2) Extend `src/lib/validation/plan.ts`:
       - `updatePlanSchema` — accepts `{ planId: z.string().uuid(), title: z.string().min(1).max(200), startDate: z.string().optional(), endDate: z.string().optional(), description: z.string().max(1000).optional() }`. Reuse field rules from createPlanSchema where possible (export a base shape and extend).
       - `archivePlanSchema` — `{ planId: z.string().uuid() }`
       - `revokeTokenSchema` — `{ tokenId: z.string().uuid() }`
       - `renameTokenSchema` — `{ tokenId: z.string().uuid(), name: z.string().min(1).max(80) }`

    3) Implement `updatePlan(formData: FormData)` in `src/server/actions/plan.ts`:
       - Parse with updatePlanSchema → on fail return `{ error: parsed.error.flatten() }`
       - getRequiredUser; cookie store; RLS-bound supabase client
       - `from('plans').update({ title, start_date, end_date, description, updated_at: new Date().toISOString() }).eq('id', planId).select().single()` — RLS plans_update_owner_only enforces ownership; non-owner attempts return zero rows updated
       - Use `revalidatePath(`/plan/${plan.slug}`); revalidatePath('/me')` so the change is visible immediately

    4) Implement `archivePlan(formData: FormData)`:
       - Parse with archivePlanSchema
       - getRequiredUser; RLS-bound client
       - `from('plans').update({ archived_at: new Date().toISOString() }).eq('id', planId).select('slug').single()` — RLS enforces owner; throws if non-owner
       - `revalidatePath('/me')`; `redirect('/me')` — owner is bounced back to dashboard after archive
       - NO hard delete: RESEARCH §Open Question 5 + threat T-02-07

    5) Implement `revokeInviteToken(formData: FormData)` in `src/server/actions/invite-token.ts`:
       - Parse with revokeTokenSchema
       - getRequiredUser; RLS-bound client
       - `from('invite_tokens').update({ revoked_at: new Date().toISOString() }).eq('id', tokenId).select('plan_id, plans(slug)').single()` — RLS invite_tokens_update_owner enforces ownership
       - `revalidatePath(`/plan/${row.plans.slug}/settings`)` so the row disappears from the list
       - D-04 semantics: existing anonymous sessions remain valid until cookie expires; only future /i/[token] sign-ins fail (already handled in Plan 01-03 invite handler — the revoked_at check)

    6) Implement `renameInviteToken(formData: FormData)`:
       - Parse with renameTokenSchema
       - getRequiredUser; RLS-bound client
       - `from('invite_tokens').update({ name }).eq('id', tokenId).select('plan_id, plans(slug)').single()`
       - `revalidatePath(`/plan/${row.plans.slug}/settings`)`

    7) `tests/integration/revoke-token.test.ts`:
       - Seed: use Plan 01-02 seed token `seedvalidtoken22charsX1`
       - Sign in as owner test user via bypass
       - Call `revokeInviteToken` Server Action with the seed token's id
       - Assert: a fresh GET to `/api/invite/seedvalidtoken22charsX1` (via the route handler) returns 302 to `/errors/token-revoked`
       - Assert DB: `from('invite_tokens').select('revoked_at').eq('token', 'seedvalidtoken22charsX1')` returns non-null revoked_at
       - Non-owner test: sign in as the second seed user (`00000000-0000-0000-0000-000000000088`) and attempt to revoke the same token; assert action throws OR returns the seed token still unrevoked (RLS blocks the update; the `.single()` call will reject because zero rows matched)
       - Cleanup: clear revoked_at on the seed token via service-role for subsequent tests

    8) `tests/integration/archive-plan.test.ts`:
       - Owner archives the seed plan via archivePlan action
       - Assert: getMyPlans(supabase, ownerUserId) excludes the seed plan (archived_at filter from Plan 01-05)
       - Assert: direct URL `/plan/seed-plan` still resolves to a 200 page (data preserved) — fetch the plan via service-role for the assertion (since RLS would still allow owner to see it; we just confirm the row is present)
       - Cleanup: clear archived_at on seed plan via service-role
  </action>
  <verify>
    <automated>pnpm drizzle-kit generate 2>&1 | tail -5 && pnpm drizzle-kit migrate 2>&1 | tail -3 && pnpm exec tsc --noEmit && pnpm test:unit --run tests/integration/revoke-token.test.ts tests/integration/archive-plan.test.ts 2>&1 | tail -25 && grep -E "invite_tokens.*ADD COLUMN.*name" drizzle/migrations/0002_invite_token_name.sql && grep -E "archived_at" src/server/actions/plan.ts && grep -E "revoked_at" src/server/actions/invite-token.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit` exits 0
    - `pnpm test:unit --run tests/integration/revoke-token.test.ts tests/integration/archive-plan.test.ts` exits 0
    - `drizzle/migrations/0002_invite_token_name.sql` contains `ALTER TABLE` and `ADD COLUMN.*name`
    - `psql "$DATABASE_MIGRATION_URL" -c "SELECT column_name FROM information_schema.columns WHERE table_name='invite_tokens' AND column_name='name'" -t | tr -d ' '` returns `name`
    - `grep -c "archivePlanSchema\\|updatePlanSchema\\|revokeTokenSchema\\|renameTokenSchema" src/lib/validation/plan.ts` returns ≥ 4
    - `grep -c "service-role\\|createServiceRoleClient" src/server/actions/plan.ts src/server/actions/invite-token.ts` returns 0 (no service-role in mutation actions — RLS is source of truth)
    - `grep -c "use server" src/server/actions/plan.ts` returns 1
    - `grep -E "revalidatePath" src/server/actions/plan.ts src/server/actions/invite-token.ts | wc -l` ≥ 3
    - Confirm: a revoked seed token returns 302 to /errors/token-revoked through the invite handler (proven by revoke-token.test.ts assertion)
  </acceptance_criteria>
  <done>Schema migration applied (invite_tokens.name nullable). Server Actions for updatePlan, archivePlan, revokeInviteToken, renameInviteToken all use RLS-bound clients. Integration tests prove revoke + archive lifecycles end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Plan settings page (Surface 6) + components + getPlanBySlug archive filter (T-06-06) + un-skip E2E specs</name>
  <files>src/app/[locale]/(app)/plan/[slug]/settings/page.tsx, src/components/plan/settings/PlanDetailsForm.tsx, src/components/plan/settings/InviteTokensSection.tsx, src/components/plan/settings/TokenRow.tsx, src/components/plan/settings/InlineConfirm.tsx, src/components/plan/settings/PlanStatusSection.tsx, src/components/plan/settings/ArchiveDialog.tsx, src/lib/db/queries/plans.ts, tests/integration/archived-plan-access.test.ts, tests/e2e/token-revoke.spec.ts, tests/e2e/archive-plan.spec.ts</files>
  <read_first>
    - .planning/phases/01-spine-plan-lifecycle/01-UI-SPEC.md §Surface 6 (full anatomy + states + accessibility + responsive)
    - .planning/phases/01-spine-plan-lifecycle/01-CONTEXT.md (specifics: archive vs delete behavior; D-02 multiple tokens per plan with per-token revoke)
    - src/components/ui/{button,card,dialog,input,label,textarea}.tsx (Plan 01-01)
    - src/components/plan/CreatePlanForm.tsx (Plan 01-04 — pattern for the details form)
    - src/server/actions/plan.ts, src/server/actions/invite-token.ts (Task 1 — actions to call)
    - src/lib/db/queries/plans.ts (Plan 01-02 — getPlanBySlug must be updated for T-06-06)
  </read_first>
  <action>
    0) **[T-06-06 MITIGATION]** Update `src/lib/db/queries/plans.ts` `getPlanBySlug` to filter archived plans for non-owners. The current signature is `getPlanBySlug(supabase, slug)`. New signature: `getPlanBySlug(supabase, slug, options?: { allowArchived?: boolean })`. Implementation:
       - Default `allowArchived = false`
       - Build the query: `supabase.from('plans').select('*').eq('slug', slug)`
       - If `!allowArchived`: append `.is('archived_at', null)`
       - Return `.maybeSingle()`
       Callers must explicitly opt in for archived access. Update the two existing call sites:
       - `src/app/[locale]/(app)/plan/[slug]/page.tsx` (Plan 01-03): leave default — anonymous and authenticated non-owners get 404 on archived plans. But OWNER must still see archived plans to access settings/restore. Refactor the page to: (a) call `getPlanBySlug(supabase, slug)` first (default, archive-filtered); (b) if null AND the requester is authenticated, retry with `allowArchived: true` AND assert `plan.ownerId === user.id` before rendering; (c) if still no match or non-owner, `notFound()`.
       - `src/app/[locale]/(app)/plan/[slug]/settings/page.tsx` (this plan, step 1 below): call with `{ allowArchived: true }` because owner must be able to reach settings for archived plans (to view history, future restore feature).

    1) `src/app/[locale]/(app)/plan/[slug]/settings/page.tsx` — RSC:
       - `const user = await getRequiredUser(await cookies(), `/plan/${slug}/settings`)`
       - `const supabase = createServerClient(await cookies())`
       - `const plan = await getPlanBySlug(supabase, slug, { allowArchived: true })` — owner can reach settings for archived plans (T-06-06 owner-bypass)
       - If `!plan` → notFound()
       - If `plan.ownerId !== user.id` → `redirect(`/plan/${slug}`)` (Surface 6 owner gate)
       - Fetch invite_tokens for this plan: `from('invite_tokens').select('id, token, name, role, created_at, use_count, revoked_at').eq('plan_id', plan.id).is('revoked_at', null).order('created_at', { ascending: false })` — RLS invite_tokens_select_owner enforces; revoked rows already filtered out
       - Page anatomy: sticky header with back chevron (`<Link href={`/plan/${slug}`}><ChevronLeft />…</Link>` 44×44px touch target) + "Configuración" title; `<div className="px-4 space-y-6">` containing the three sections
       - `<PlanDetailsForm plan={plan} />` (client island)
       - `<InviteTokensSection planId={plan.id} tokens={tokens} />` (client island — manages list state + generate-new flow)
       - `<PlanStatusSection planId={plan.id} planSlug={plan.slug} />` (client island — opens archive/delete dialogs)
       - `export const metadata = { robots: 'noindex' }`

    2) `src/components/plan/settings/PlanDetailsForm.tsx` — `'use client'`. Mirrors CreatePlanForm shape but pre-populates fields from props.plan. Submit calls `updatePlan` Server Action with FormData (including hidden `planId`). On success: show inline success indicator (no toast — Surface 2 rule generalized) for 2 seconds; on error: form-level error banner with `t('common.error_generic')`. Use Zod resolver with updatePlanSchema (re-exported from validation).

    3) `src/components/plan/settings/InviteTokensSection.tsx` — `'use client'`. Manages the list of token rows:
       - Section header: `<h2 className="text-xl font-semibold text-zinc-950">{t('plan.settings.invites_active', { count: tokens.length })}</h2>`
       - Generate CTA: outline button full-width with lucide `Plus` icon + `t('plan.settings.generate_link')`; onclick calls `mintInviteToken(planId, 'viewer')` Server Action (already exists from Plan 01-04). On success, the page revalidates via `revalidatePath` and the new row appears at top
       - Token list: `<ul role="list" className="space-y-3 mt-4">{tokens.map(t => <TokenRow key={t.id} token={t} planSlug={...} />)}</ul>`
       - The "name" defaults: if token.name is null, compute display name as `t('plan.settings.token_default_name', { number: position })` where position is the 1-based reverse-chronological index. Pass the computed default to TokenRow as a fallback for the editable name field

    4) `src/components/plan/settings/TokenRow.tsx` — `'use client'`. Props `{ token, defaultName, planSlug }`. State: `isEditingName`, `isConfirmingRevoke`.
       - Row card: `p-4 rounded-lg shadow-sm bg-white`
       - Editable name: when not editing, render `<button onClick=>{setIsEditingName(true)}>{token.name ?? defaultName}</button>`. When editing, render `<Input>` with initial value, onBlur or onKeyDown(Enter) calls renameInviteToken Server Action then setIsEditingName(false). On Escape: revert without saving.
       - Meta line: `text-sm text-zinc-500`: `t('plan.settings.token_created', { date: formatDate(token.created_at, locale) })` + ' · ' + `t('plan.settings.token_used', { count: token.use_count })`
       - Revoke action: when not confirming → text-button red-600 `t('plan.settings.token_revoke')`; when confirming → render `<InlineConfirm>` (see below)
       - Touch target: every interactive element 44px minimum (UI-SPEC mobile rule)
       - After successful revoke: row removes via parent component re-render; add a 200ms opacity fade-out before unmount using `useTransition` or a local `isRemoving` state

    5) `src/components/plan/settings/InlineConfirm.tsx` — generic component. Props `{ message: string; confirmLabel: string; cancelLabel: string; onConfirm: () => Promise<void>; onCancel: () => void; destructive?: boolean }`. Renders inside the row, NOT a modal. Layout: copy line + two stacked buttons (destructive red filled + ghost cancel). `role="alert"` for screen-reader announcement on expansion. Focus on Cancel button by default (UI-SPEC §Modal/Sheet Base Rules safe-default rule). Cancel returns focus to the original trigger.

    6) `src/components/plan/settings/PlanStatusSection.tsx` — `'use client'`. Two destructive buttons:
       - `<Button variant="outline" className="border-red-200 text-red-600 hover:bg-red-50 w-full" onClick=>{openArchive(true)}>{t('plan.settings.archive_button')}</Button>` — opens `<ArchiveDialog kind="archive" />`
       - `<Button variant="ghost" className="text-zinc-500 hover:bg-zinc-100 w-full mt-2 text-sm" onClick=>{openDelete(true)}>{t('plan.settings.delete_button')}</Button>` — opens `<ArchiveDialog kind="delete" />` (same component, stronger copy)
       Section header: `<h2 className="text-xl font-semibold text-zinc-950">{t('plan.settings.section_status')}</h2>`

    7) `src/components/plan/settings/ArchiveDialog.tsx` — `'use client'`. Props `{ open, onOpenChange, planId, planSlug, kind: 'archive' | 'delete' }`. Renders shadcn `<Dialog>`:
       - Title: `kind === 'archive' ? t('plan.settings.archive_dialog_title') : t('plan.settings.delete_dialog_title')`
       - Copy: `kind === 'archive' ? t('plan.settings.archive_dialog_copy') : t('plan.settings.delete_dialog_copy')`
       - Confirm button: red filled, `kind === 'archive' ? t('plan.settings.archive_confirm') : t('plan.settings.delete_confirm')`; onclick calls `archivePlan(planId)` Server Action (BOTH archive AND delete call the same action — soft-delete only per RESEARCH §Open Question 5)
       - Cancel button: ghost, `t('common.cancel')`
       - Focus on Cancel by default (UI-SPEC §Modal/Sheet Base Rules)
       - Backdrop: bg-zinc-950/60
       - On success: navigate to /me (the archivePlan action already calls redirect)

    8) Un-skip `tests/e2e/token-revoke.spec.ts`. Replace `describe.skip` with active tests. Pre-test setup: bypass-auth as owner test user. Steps:
       - Goto `/plan/seed-plan/settings`
       - Assert ≥ 1 token row visible
       - Click "Revocar link" on the seed valid token row
       - Assert inline confirm appears with `t('plan.settings.token_revoke_confirm_copy')` text
       - Click the destructive confirm button
       - Assert the row disappears from the list (with optional 200ms fade)
       - In a fresh incognito context, goto `/i/seedvalidtoken22charsX1`
       - Assert URL contains `/errors/token-revoked`
       - Assert the error page title is visible
       Cleanup: clear revoked_at on the seed token via service-role.

    10) Create `tests/integration/archived-plan-access.test.ts` — **T-06-06 mitigation verification**. Vitest. Uses local Supabase. Test cases:
       - Archive the seed plan via service-role (`update plans set archived_at = now() where slug = 'seed-plan'`)
       - **Case A** (anonymous with valid plan_id JWT claim): mint an anonymous user with `app_metadata.plan_id = seed plan id`; call `getPlanBySlug(supabase, 'seed-plan')` (default, archive-filtered); assert returns `null` — anonymous viewers must NOT see archived plans even with a valid claim
       - **Case B** (authenticated non-owner): sign in as the second seed user (`00000000-0000-0000-0000-000000000088`); add them as a plan_member via service-role; call `getPlanBySlug(supabase, 'seed-plan')` (default); assert returns `null` — non-owners (even members) must NOT see archived plans
       - **Case C** (authenticated owner): sign in as the owner test user; call `getPlanBySlug(supabase, 'seed-plan', { allowArchived: true })`; assert returns the plan row (owner bypass works)
       - **Case D** (authenticated owner, default args): sign in as owner; call `getPlanBySlug(supabase, 'seed-plan')` (default, no opt-in); assert returns `null` — confirms the default is archive-filtered even for owner unless `allowArchived: true` is passed (defensive — forces explicit opt-in at the call site)
       Cleanup: clear archived_at on the seed plan + remove the test plan_members row.

    9) Create `tests/e2e/archive-plan.spec.ts`. Pre-test setup: bypass-auth as owner. Steps:
       - Goto `/plan/seed-plan/settings`
       - Click "Archivar plan"
       - Assert dialog visible with archive copy
       - Click confirm
       - Assert URL is `/me` (archivePlan redirects)
       - Assert the seed plan card is NOT visible on /me (empty state or other plans only)
       - Direct-URL test: goto `/plan/seed-plan` as the owner; assert the page still loads (owner bypass works)
       - **T-06-06 verification**: open `/plan/seed-plan` in a fresh incognito context (no JWT) — assert returns 404 (RLS blocks anon-no-claim). Then open with the original anon JWT (still in WhatsApp's webview, claim still references the now-archived plan) — assert returns 404 because of the archived_at filter in `getPlanBySlug`, NOT because RLS rejects (RLS still permits anon-with-claim to SELECT the row; the application-layer filter is what hides archived plans).
       Cleanup: clear archived_at via service-role.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm check && pnpm build 2>&1 | tail -10 && pnpm test:unit --run tests/integration/archived-plan-access.test.ts 2>&1 | tail -10 && pnpm test:e2e --project=chromium-desktop tests/e2e/token-revoke.spec.ts tests/e2e/archive-plan.spec.ts 2>&1 | tail -25 && pnpm test:e2e --project=webkit-mobile tests/e2e/token-revoke.spec.ts tests/e2e/archive-plan.spec.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit` exits 0
    - `pnpm check` exits 0
    - `pnpm build` exits 0
    - `pnpm test:e2e tests/e2e/token-revoke.spec.ts` exits 0 on chromium-desktop AND webkit-mobile
    - `pnpm test:e2e tests/e2e/archive-plan.spec.ts` exits 0 on both projects
    - `grep -c "redirect.*plan.*slug" "src/app/[locale]/(app)/plan/[slug]/settings/page.tsx"` returns ≥ 1 (non-owner redirect — pattern may use template literal)
    - `grep -E "owner_id|ownerId" "src/app/[locale]/(app)/plan/[slug]/settings/page.tsx"` matches (owner gate)
    - `grep -E "robots.*noindex" "src/app/[locale]/(app)/plan/[slug]/settings/page.tsx"` matches
    - `grep -c "use client" src/components/plan/settings/{TokenRow,InlineConfirm,PlanDetailsForm,InviteTokensSection,PlanStatusSection,ArchiveDialog}.tsx` returns 6 (all client components)
    - `grep -rE "\"[A-ZÁÉÍÓÚÑa-záéíóúñ]{4,}\"" src/components/plan/settings src/app/[locale]/[(]app[)]/plan/[\\[]slug[\\]]/settings | grep -v 'src/lib/i18n' | grep -v -E '^[^:]+:\s*//' | wc -l` returns 0
    - `grep -E "role=.alert." src/components/plan/settings/InlineConfirm.tsx` matches (a11y)
    - `grep -E "44px|min-h-\[44px\]|h-11|h-12|h-14" src/components/plan/settings/TokenRow.tsx` matches (touch target)
    - `grep -E "archived_at|allowArchived" src/lib/db/queries/plans.ts` matches (T-06-06 filter present)
    - `grep -cE "allowArchived" src/lib/db/queries/plans.ts` returns ≥ 1 (opt-in param exists)
    - `pnpm test:unit --run tests/integration/archived-plan-access.test.ts` exits 0 with ≥ 4 assertions (the 4 cases A-D in step 10)
    - All Phase 1 critical-path E2E flows (VALIDATION.md §Critical Paths 1–6) pass — final phase verification check
  </acceptance_criteria>
  <done>Surface 6 fully implemented; owner can edit details, mint/rename/revoke tokens, and archive plans; all destructive actions use inline confirm (tokens) or dialog (plan archive/delete) with focus-on-cancel safe default; revoked tokens 404 the public /i/[token] route; archived plans disappear from /me but data is preserved; final un-skipped E2E specs round out the Phase 1 test suite.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final Phase 1 verification — exercise all 6 critical paths end-to-end</name>
  <files>(none — manual verification task; no file modifications)</files>
  <action>Human runs the 6 critical-path checks listed in <how-to-verify>.</action>
  <verify>Run `pnpm test:unit --run && pnpm test:e2e --project=chromium-desktop && pnpm test:e2e --project=webkit-mobile` — all green.</verify>
  <done>All 6 critical paths pass; VALIDATION.md flipped to nyquist_compliant: true.</done>
  <what-built>
    Phase 1 is complete: all 11 requirements implemented, all 23 CONTEXT.md decisions land in code, both human-required setups (Custom Access Token Hook toggle in Plan 01-02; Google OAuth provider config in Plan 01-04) are done, all critical paths automated. This checkpoint runs the full VALIDATION.md §Critical Paths matrix to confirm phase verification readiness before /gsd:verify-phase.
  </what-built>
  <how-to-verify>
    Run the full VALIDATION.md §Critical Paths suite. For each, document outcome:

    **Path 1 — Walking Skeleton:** `pnpm test:e2e tests/e2e/walking-skeleton.spec.ts` exits 0. Manual: `pnpm dev`, open localhost:3000, see marketing landing in Spanish.

    **Path 2 — Plan create + share + view (anon):** Manual (or `pnpm test:e2e tests/e2e/create-plan.spec.ts` + `tests/e2e/anon-link-view.spec.ts`):
    1. Sign in with Google (real OAuth) → /me
    2. Click "Crear plan" → /plan/new
    3. Type "Despedida E2E" → submit
    4. Land on /plan/[slug]?share=1; share dialog auto-opens
    5. Copy /i/[token]
    6. Paste link in incognito → land on /plan/[slug] with anon view + empty state + sign-in bar

    **Path 3 — Hybrid OAuth upgrade:** Manual (Plan 01-05 Task 3 checkpoint repeated):
    1. From step 6 above, tap "Continuar con Google" in sign-in bar
    2. Complete real OAuth
    3. Land back on /plan/[slug] with avatar in header (no toast per D-14)
    4. Verify in DB Studio: plan_members row inserted with role 'viewer' + joined_via_token_id set

    **Path 4 — Token revoke:** Automated `pnpm test:e2e tests/e2e/token-revoke.spec.ts` PLUS manual:
    1. As owner, goto /plan/[slug]/settings
    2. Revoke the just-shared token
    3. Open the same /i/[token] in a fresh incognito → land on /errors/token-revoked

    **Path 5 — Archive plan:** Automated `pnpm test:e2e tests/e2e/archive-plan.spec.ts` PLUS manual:
    1. As owner, archive a plan
    2. Confirm it disappears from /me
    3. Open the direct /plan/[slug] URL → confirm the page still loads (RLS still allows owner)
    4. Open the /plan/[slug] URL in incognito with NO token → confirm it 404s (RLS blocks anon-no-claim)

    **Path 6 — Cross-RLS isolation:** Automated `pnpm test:unit --run tests/integration/rls-*.test.ts`:
    - All 4 RLS integration test files pass

    **Run the full suite once:** `pnpm test:unit --run && pnpm test:e2e --project=chromium-desktop && pnpm test:e2e --project=webkit-mobile` — ALL should be green.

    **VALIDATION.md cleanup:** Update VALIDATION.md frontmatter to `nyquist_compliant: true` and tick all rows in the per-task verification table.

    **Manual smoke for the items in VALIDATION.md §Manual-Only Verifications:**
    - Re-verify the OG preview in WhatsApp (Plan 01-04 Task 3 already covered this; just confirm it still works after all the other plans landed)
    - Audit the sign-in bottom sheet visually for the absence of any Apple Sign In affordance (AUTH-03 deferred)
    - Read all es.json keys and confirm neutral Spanish tone (no regional slang)
    - Empty state copy: confirm the creator name interpolates correctly for long names (e.g. "María del Carmen García-Hernández")
  </how-to-verify>
  <resume-signal>Type "approved" only after ALL 6 critical paths pass AND the manual-only items in VALIDATION.md are confirmed. Phase 1 is then ready for `/gsd:verify-phase 1`. If anything fails, describe which path and the failure mode — a gap-closure plan (`/gsd:plan-phase 1 --gaps`) can address it.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Non-owner authenticated user → /plan/[slug]/settings | Page-level gate (redirect) + RLS-level gate (Server Actions reject on zero-row update) |
| Owner client → revokeInviteToken | RLS-bound; service-role NOT used in this path |
| Owner client → archivePlan | Same — RLS is source of truth; archived_at update is allowed by plans_update_owner_only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | Non-owner forges revokeInviteToken via Server Action call | mitigate | RLS invite_tokens_update_owner enforces ownership; the .single() call rejects when zero rows match; page-level redirect is defense-in-depth |
| T-06-02 | Repudiation | Token revoke / plan archive history lost | mitigate | revoked_at + archived_at are SET-only via RLS UPDATE; no DELETE policy on invite_tokens or plans (Plan 01-02); audit trail preserved (D-04, D-05) |
| T-06-03 | Tampering | Renaming a token via renameInviteToken could overwrite the token value itself | mitigate | renameTokenSchema validates `name` only; the `token` column is NOT updatable through this action; RLS UPDATE policy combined with explicit `.update({ name })` field selection |
| T-06-04 | Denial of Service | Owner repeatedly mints + revokes tokens, growing invite_tokens table unbounded | accept | Single-organizer attack is self-inflicted; no v1 quota; document Phase 7 follow-up: rate-limit mintInviteToken to e.g. 20 tokens/plan/hour |
| T-06-05 | Information Disclosure | A leaked /i/[token] is revoked but the existing anonymous sessions remain valid for ~1 week | accept | D-04 — explicit decision. Documented in CONTEXT.md. If a leak case emerges, the planned mitigation is to add a `plan_revocations` table (CONTEXT.md deferred) |
| T-06-06 | Information Disclosure | Archived plan still readable by anonymous viewers via existing JWT plan_id claim | mitigate | Task 2 step 0 updates `getPlanBySlug(supabase, slug, options?)` to default-filter `archived_at IS NULL`. Owner must explicitly pass `{ allowArchived: true }` to reach archived plans (only the settings page does this). Anonymous and non-owner authenticated viewers receive `null` from the query → notFound() on archived plans. Verified by `tests/integration/archived-plan-access.test.ts` (4 cases: anon, non-owner, owner-with-opt-in, owner-default). |

</threat_model>

<verification>
- Owner revokes token → revoked /i/[token] returns 302 to /errors/token-revoked (PLAN-04)
- Owner archives plan → plan disappears from /me; direct URL still resolves for owner; non-owner gets 404 (PLAN-05)
- Surface 6 anatomy matches UI-SPEC: 3 sections, owner gate, inline confirm for token revoke (no modal), dialog for plan archive/delete with focus-on-cancel
- All 6 VALIDATION.md critical paths pass
- VALIDATION.md frontmatter set to nyquist_compliant: true
</verification>

<success_criteria>
- PLAN-04 + PLAN-05 fully delivered
- All 11 Phase 1 requirements satisfied across plans 01-01 → 01-06
- All 6 critical-path E2E flows pass on chromium-desktop + webkit-mobile
- RLS enforces every mutation (no service-role bypass in revoke/archive paths)
- Soft-delete only; no hard delete capability shipped in Phase 1
- Final manual checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/01-spine-plan-lifecycle/01-06-SUMMARY.md` when done. Include:
- Confirmation that all 6 critical-path E2E flows passed
- The exact behavior of "Eliminar plan" (Surface 6 §Delete copy explicitly maps to archive — confirm executor implemented this correctly)
- Whether archived plans gracefully 404 for non-owners (T-06-06 mitigation applied to getPlanBySlug)
- VALIDATION.md update commit hash or confirmation that nyquist_compliant flipped to true
- Phase 1 final readiness summary: requirement coverage table, decision coverage (D-01 through D-23), and the green-light for /gsd:verify-phase 1
</output>
