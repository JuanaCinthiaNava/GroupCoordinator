---
phase: 01-spine-plan-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - biome.json
  - playwright.config.ts
  - vitest.config.ts
  - drizzle.config.ts
  - components.json
  - postcss.config.mjs
  - .env.local.example
  - .gitignore
  - supabase/config.toml
  - src/app/layout.tsx
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/(marketing)/layout.tsx
  - src/app/[locale]/(marketing)/page.tsx
  - src/app/[locale]/(app)/layout.tsx
  - src/app/global-error.tsx
  - src/middleware.ts
  - src/i18n/routing.ts
  - src/i18n/request.ts
  - src/lib/i18n/messages/es.json
  - src/lib/i18n/messages/en.json
  - src/lib/i18n/messages/pt.json
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/form.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - src/components/ui/select.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/sonner.tsx
  - src/components/ui/tabs.tsx
  - src/components/ui/toast.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/avatar.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/command.tsx
  - src/components/ui/textarea.tsx
  - src/components/Logo.tsx
  - src/styles/globals.css
  - tests/setup/supabase.ts
  - tests/setup/auth.ts
  - tests/unit/token.test.ts
  - tests/unit/i18n-keys.test.ts
  - tests/e2e/walking-skeleton.spec.ts
  - tests/e2e/anon-link-view.spec.ts
  - tests/e2e/oauth-upgrade.spec.ts
  - tests/e2e/token-revoke.spec.ts
  - public/robots.txt
autonomous: false
requirements: []
truths:
  - "Repo bootstraps to a working Next.js 15.5 app: `pnpm dev` serves /es and /en without errors"
  - "Local Supabase running via `pnpm supabase start` — Studio reachable at http://localhost:54323"
  - "Biome replaces ESLint — `pnpm check` exits 0 on a clean tree"
  - "shadcn components installed; Geist Sans + Geist Mono load via next/font/google"
  - "next-intl scaffold reads from src/lib/i18n/messages/es.json; <Logo /> renders the Spanish wordmark from logo.wordmark key"
  - "No string literal in any .tsx file outside src/lib/i18n/messages/ — Biome ignore allowlist documents legitimate exceptions"
  - "Wave 0 test scaffold present: vitest.config.ts, playwright.config.ts, four E2E stub specs, two unit specs"
  - "robots.txt Disallows /plan/* and /i/* (D-22)"
must_haves:
  truths:
    - "Toolchain assembled and verifiable in one local command sequence"
    - "Marketing landing skeleton at / proves routing + i18n + design tokens render"
    - "Wave 0 test harness gates all future work — no task ships without a green or wave-0-deferred verify"
  artifacts:
    - path: package.json
      provides: "Workspace manifest with all Phase 1 dependencies and scripts (dev, build, lint, check, format, test:unit, test:e2e, db:generate, db:migrate, db:push)"
      contains: "drizzle-orm, drizzle-kit, @supabase/ssr, @supabase/supabase-js, next-intl, react-hook-form, @hookform/resolvers, zod, nanoid, lucide-react, sonner, vitest, @playwright/test, @biomejs/biome, next@15.5"
    - path: src/middleware.ts
      provides: "next-intl middleware + matcher that excludes /api, /_next, /_vercel, static assets"
      contains: "createMiddleware"
    - path: src/app/[locale]/(marketing)/page.tsx
      provides: "Marketing landing skeleton — Surface 8 from UI-SPEC (logo lg + tagline + CTA + footer)"
      contains: "useTranslations"
    - path: supabase/config.toml
      provides: "Local Supabase config — Postgres on 54322, Studio on 54323, API on 54321"
    - path: drizzle.config.ts
      provides: "Drizzle Kit config — schema=./drizzle/schema.ts, out=./drizzle/migrations, dialect=postgresql, dbCredentials uses DATABASE_MIGRATION_URL"
    - path: tests/e2e/walking-skeleton.spec.ts
      provides: "Stub spec gating Plan 01-04; placeholder asserts /es page renders 200"
    - path: public/robots.txt
      provides: "Disallow /plan/ and /i/ globally (D-22)"
  key_links:
    - from: src/app/[locale]/layout.tsx
      to: src/lib/i18n/messages/es.json
      via: NextIntlClientProvider + getMessages()
      pattern: "NextIntlClientProvider"
    - from: src/middleware.ts
      to: src/i18n/routing.ts
      via: createMiddleware(routing)
      pattern: "createMiddleware\\(routing\\)"
user_setup:
  - service: supabase-cli
    why: "Local Supabase stack (Postgres + Auth + Studio) for Phase 1 dev — required before Plan 01-02 can run migrations"
    env_vars: []
    dashboard_config:
      - task: "Install Supabase CLI 2.x + Docker; the Wave 0 verification runs `supabase --version` and `docker --version`"
        location: "Local machine (brew install supabase/tap/supabase, Docker Desktop)"
  - service: node-toolchain
    why: "Required runtime for Next.js 15.5 + pnpm 9"
    env_vars: []
    dashboard_config:
      - task: "Ensure node >= 20, pnpm >= 9"
        location: "Local machine"
---

<objective>
Bootstrap the Next.js 15.5 + Supabase + Drizzle + next-intl + shadcn + Biome + Vitest + Playwright project skeleton so that `pnpm dev` serves the marketing landing in Spanish, `pnpm supabase start` brings up local Postgres + Studio, and `pnpm test:unit` + `pnpm test:e2e` find the Wave 0 test harness (passing the trivial walking-skeleton spec). Produces the architectural backbone described in `01-SKELETON.md`.

Purpose: Every other Phase 1 plan depends on this toolchain. No schema, no auth, no plan logic can land until the scaffold is up. This plan is intentionally heavy on configuration and light on business logic — the business-logic walking skeleton is completed by Plans 01-02 (schema + RLS) and 01-03 (anonymous link view).

Output: A repository that another developer can clone, run three commands (`pnpm install`, `pnpm supabase start`, `pnpm dev`), and see the Spanish landing at `http://localhost:3000`. Wave 0 test harness present and runnable.

User story (MVP_MODE): **As a** Phase 1 contributor, **I want to** clone the repo and run a deterministic toolchain bootstrap, **so that** I can begin shipping vertical slices on a Walking Skeleton that already proves Next.js + Supabase + i18n + design tokens integrate correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-spine-plan-lifecycle/01-CONTEXT.md
@.planning/phases/01-spine-plan-lifecycle/01-RESEARCH.md
@.planning/phases/01-spine-plan-lifecycle/01-UI-SPEC.md
@.planning/phases/01-spine-plan-lifecycle/01-VALIDATION.md
@.planning/phases/01-spine-plan-lifecycle/01-SKELETON.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- No prior code interfaces — this plan creates the initial scaffold. Executor follows RESEARCH §Area 5 Bootstrap Order step-by-step. -->

Locked design tokens from UI-SPEC.md §Design Tokens:
- accent: emerald-700 (#047857); focus ring: emerald-600 (#059669)
- chrome neutrals: zinc-50/100/200/300/500/600/950
- fonts: Geist Sans, Geist Mono via next/font/google with CSS variables --font-geist-sans, --font-geist-mono
- type scale: 14/16/20/24 px; weights 400 + 600 only
- radius: rounded-md inputs/buttons, rounded-lg cards/dialogs
- spacing scale: 4/8/12/16/24/32/48 (space-5 reserved/unused)
- 60+ es.json keys spanning namespaces: common, nav, logo, landing, plan, me, auth, og, errors (full catalog in UI-SPEC §Microcopy Catalog)

Locked package.json dependency set from RESEARCH.md §Area 5 step 3:
- @supabase/supabase-js, @supabase/ssr
- drizzle-orm, postgres
- react-hook-form, @hookform/resolvers, zod, next-safe-action
- next-intl
- nanoid, lucide-react, sonner
Dev: @biomejs/biome (--save-exact), supabase, drizzle-kit, vitest, @vitejs/plugin-react, jsdom, @testing-library/react, @playwright/test
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Next.js scaffold + Biome + Supabase + Drizzle + shadcn + next-intl wiring</name>
  <files>package.json, pnpm-lock.yaml, tsconfig.json, next.config.ts, biome.json, drizzle.config.ts, postcss.config.mjs, components.json, .env.local.example, .gitignore, supabase/config.toml, src/app/layout.tsx, src/app/[locale]/layout.tsx, src/app/[locale]/(marketing)/layout.tsx, src/app/[locale]/(marketing)/page.tsx, src/app/[locale]/(app)/layout.tsx, src/app/global-error.tsx, src/middleware.ts, src/i18n/routing.ts, src/i18n/request.ts, src/lib/i18n/messages/es.json, src/lib/i18n/messages/en.json, src/lib/i18n/messages/pt.json, src/components/Logo.tsx, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/dialog.tsx, src/components/ui/form.tsx, src/components/ui/input.tsx, src/components/ui/label.tsx, src/components/ui/select.tsx, src/components/ui/sheet.tsx, src/components/ui/sonner.tsx, src/components/ui/tabs.tsx, src/components/ui/toast.tsx, src/components/ui/tooltip.tsx, src/components/ui/dropdown-menu.tsx, src/components/ui/avatar.tsx, src/components/ui/badge.tsx, src/components/ui/command.tsx, src/components/ui/textarea.tsx, src/styles/globals.css, public/robots.txt</files>
  <read_first>
    - .planning/phases/01-spine-plan-lifecycle/01-RESEARCH.md §Area 5 (steps 1–11 — exact command sequence and config snippets)
    - .planning/phases/01-spine-plan-lifecycle/01-UI-SPEC.md §Design System, §Design Tokens, §Microcopy Catalog (copy ALL es.json keys verbatim), §Surface 8 (marketing skeleton anatomy)
    - .planning/phases/01-spine-plan-lifecycle/01-CONTEXT.md (D-15 Drizzle, D-16 Google only, D-20 next-intl, D-22 noindex + robots, D-23 Vercel cap)
    - .planning/research/STACK.md §Installation, §Recommended Stack
    - .planning/research/ARCHITECTURE.md §Recommended Project Structure (canonical directory layout)
    - .planning/phases/01-spine-plan-lifecycle/01-SKELETON.md (architectural decisions — do not deviate)
  </read_first>
  <action>
    Follow RESEARCH §Area 5 step-by-step. Concrete sub-steps with identifier targets:

    1) Run `pnpm create next-app@15.5 . --typescript --tailwind --app --eslint=false --src-dir --import-alias "@/*"` from the project root (note: directory is the existing repo root — use `.`; if create-next-app refuses, scaffold into `/tmp/group-coordinator` and rsync files in, preserving `.planning/`, `.git/`, `.gitignore`, CLAUDE.md if present).

    2) Remove any ESLint artifacts that create-next-app added (`pnpm remove eslint eslint-config-next` if present); add Biome with `pnpm add -D --save-exact @biomejs/biome` and run `pnpm biome init`. Write `biome.json` with the exact JSON in RESEARCH §Area 5 step 2 (organizeImports enabled; ignore .next/, node_modules/, drizzle/migrations/, supabase/migrations/, supabase/policies/, src/lib/i18n/messages/). Add scripts to `package.json`: `lint` → `biome lint ./src`, `format` → `biome format --write ./src`, `check` → `biome check --write ./src`.

    3) Install Phase 1 dependencies in one batch (see RESEARCH §Area 5 step 3). Add additional scripts to `package.json`: `dev` (default), `build`, `start`, `test:unit` → `vitest run`, `test:unit:watch` → `vitest`, `test:e2e` → `playwright test`, `db:generate` → `drizzle-kit generate`, `db:migrate` → `drizzle-kit migrate`, `db:push` → `supabase db push`, `db:seed` → `psql "$DATABASE_MIGRATION_URL" -f supabase/seed.sql`, `db:studio` → `drizzle-kit studio`.

    4) Initialize shadcn (`pnpm dlx shadcn@latest init`) with New York style + Zinc base + CSS variables = yes. Edit `components.json` to confirm the alias is `@/components` and base color is `zinc`. Add the Phase 1 component set listed in UI-SPEC §Design System (`button card dialog form input label select sonner tabs toast tooltip dropdown-menu avatar badge command sheet`) plus `textarea` (UI-SPEC Surface 1 uses Textarea).

    5) Create `src/styles/globals.css` with: Tailwind v4 base, custom CSS variables for every color token in UI-SPEC §Design Tokens (--color-background through --color-ring), Geist Sans/Mono font-family declarations referencing the next/font CSS variables, and the focus-visible rule (ring-2 ring-[#059669] ring-offset-2). Import this from `src/app/layout.tsx`.

    6) Configure Geist fonts in `src/app/layout.tsx` via `import { Geist, Geist_Mono } from 'next/font/google'` with variable names `--font-geist-sans` and `--font-geist-mono`. Set `<html lang="es">` will be overridden by per-locale layout; root layout sets the variables on `<body>` and renders `<html>` with `suppressHydrationWarning`.

    7) Scaffold next-intl: write `src/i18n/routing.ts` exactly as RESEARCH §Area 5 step 9 (locales ['es','en','pt'], defaultLocale 'es', localePrefix 'as-needed'); write `src/i18n/request.ts` exporting the message loader from `src/lib/i18n/messages/{locale}.json`; write `src/middleware.ts` with `createMiddleware(routing)` and the matcher `['/((?!api|_next|_vercel|.*\\..*).*)'`. Update `next.config.ts` to wrap config with `next-intl/plugin` pointing at `./src/i18n/request.ts`.

    8) Write the FULL es.json catalog from UI-SPEC §Microcopy Catalog into `src/lib/i18n/messages/es.json` — every key under common, nav, logo, landing, plan (create, share_dialog, view, settings), me, auth, og, errors. Copy bytes-exact strings; do not paraphrase. Create `en.json` and `pt.json` as identical copies (stubbed — per D-20, translation deferred).

    9) Create the locale layout chain:
       - `src/app/[locale]/layout.tsx` — wraps children in `<NextIntlClientProvider>` after `setRequestLocale(locale)` and `getMessages()`. Sets `<html lang={locale}>`. Validates locale against `routing.locales` or calls `notFound()`.
       - `src/app/[locale]/(marketing)/layout.tsx` — minimal (no header), just renders children.
       - `src/app/[locale]/(app)/layout.tsx` — placeholder for Plan 01-04's app shell; for now exports a passthrough RSC that renders children. Mark TODO for Plan 01-04 to add the sticky 56px header.
       - `src/app/[locale]/(marketing)/page.tsx` — implements UI-SPEC Surface 8 anatomy: centered flex column, `<Logo size="lg" />`, tagline (`t('landing.tagline')`), sub-copy (`t('landing.sub_tagline')`), CTA (`t('landing.cta_create')`) linking to `/plan/new`, footer (`t('landing.footer')`). Uses Tailwind classes only; no inline styles.

    10) Write `src/components/Logo.tsx` with the locked `<Logo size="sm | md | lg" />` prop API from UI-SPEC §Logo Treatment. Renders text wordmark (Geist Sans semibold, sizes 14/16/20px) using `t('logo.wordmark')` for `md`/`lg` and `t('logo.chip')` for `sm`. Marked with `'use client'` only if it needs `useTranslations` from `next-intl` client API — prefer the RSC import path (`getTranslations`) for server use.

    11) Configure local Supabase: run `pnpm supabase init` to write `supabase/config.toml`. Edit `supabase/config.toml` to set `[auth.external.google] enabled = true` and `redirect_uri = "http://localhost:54321/auth/v1/callback"`; set `[auth.hook.custom_access_token] enabled = true` and `uri = "pg-functions://postgres/public/custom_access_token_hook"` (the function itself is created in Plan 01-02; the toml registers the hook).

    12) Write `drizzle.config.ts` using `DATABASE_MIGRATION_URL` env var (NOT `DATABASE_URL`) so migrations connect via direct port 5432 / 54322. Schema path `./drizzle/schema.ts` (file to be created in Plan 01-02), out `./drizzle/migrations`, dialect `postgresql`.

    13) Write `.env.local.example` with all keys from RESEARCH §Area 5 step 6 (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, DATABASE_URL, DATABASE_MIGRATION_URL, NEXT_PUBLIC_SITE_URL). Comment that real values come from `supabase status`. Add `.env.local` to `.gitignore`.

    14) Write `public/robots.txt` with `User-agent: *` then `Disallow: /plan/` and `Disallow: /i/` (D-22). Add a security comment line explaining the directive.

    15) Write `src/app/global-error.tsx` as a minimal Next.js global error boundary (required in App Router; renders `t('errors.server_error')` once i18n is wired, or a hardcoded Spanish fallback for the root-error case where the i18n provider may not have mounted — the only allowlist exception, documented in `biome.json` ignore comment).

    16) DO NOT use Math.random anywhere. DO NOT hardcode Spanish strings in tsx outside `src/lib/i18n/messages/`.
  </action>
  <verify>
    <automated>pnpm install && pnpm check && pnpm exec tsc --noEmit && pnpm build 2>&1 | tee /tmp/01-01-task1.log | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` exits 0 with `next@15.5.x`, `drizzle-orm@^0.36.x`, `@supabase/ssr`, `next-intl`, `nanoid`, `@biomejs/biome` (exact pin) present in package.json
    - `pnpm check` exits 0 reporting zero violations
    - `pnpm exec tsc --noEmit` exits 0
    - `pnpm build` exits 0 and produces `.next/` with both `/es` (or default `/`) and `/en/` route segments compiled
    - `grep -rE "\"[A-ZÁÉÍÓÚÑa-záéíóúñ]{4,}\"" src/app src/components | grep -v 'src/lib/i18n' | grep -v -E '^[^:]+:\s*//' | wc -l` returns 0 (allowlist: global-error.tsx Spanish fallback noted in biome.json)
    - `cat src/lib/i18n/messages/es.json | jq '.plan.view.empty_heading'` returns the string `"{creator} sigue agregando detalles. Vuelve pronto."` exactly (D-08)
    - `cat src/lib/i18n/messages/es.json | jq -r 'paths(scalars) | length' | sort -u` reports paths of depth 2–3 (catalog is namespaced)
    - `node -e "const es=require('./src/lib/i18n/messages/es.json'); const en=require('./src/lib/i18n/messages/en.json'); const pt=require('./src/lib/i18n/messages/pt.json'); const keys=o=>{const out=[];const walk=(v,p)=>{if(typeof v==='object'&&v!==null)for(const k in v)walk(v[k],p?p+'.'+k:k);else out.push(p)};walk(o,'');return out.sort()}; const ek=keys(es).join(); if(keys(en).join()!==ek||keys(pt).join()!==ek){console.error('KEY MISMATCH');process.exit(1)} console.log('OK')"` outputs `OK`
    - `cat public/robots.txt | grep -E '^Disallow: /(plan|i)/' | wc -l` returns 2
    - `grep -E 'createMiddleware\(routing\)' src/middleware.ts` matches
    - `cat supabase/config.toml | grep -E 'enabled = true' | grep -c custom_access_token` returns 1
    - `ls src/components/ui/{button,card,dialog,form,input,label,select,sheet,sonner,tabs,toast,tooltip,dropdown-menu,avatar,badge,command,textarea}.tsx` lists 17 files
    - `<Logo />` prop API: `grep -E 'size.*[\"\']sm[\"\']' src/components/Logo.tsx` matches AND `grep -E 'size.*[\"\']md[\"\']' src/components/Logo.tsx` matches AND `grep -E 'size.*[\"\']lg[\"\']' src/components/Logo.tsx` matches
  </acceptance_criteria>
  <done>Repository scaffolded, dependencies pinned, Biome green, type-check clean, build succeeds, marketing landing renders, next-intl scaffold reads all three locales, no hardcoded Spanish strings outside the i18n catalog, robots.txt locks down /plan and /i paths.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 test harness — Vitest + Playwright config, stub specs, test bypass helpers</name>
  <files>vitest.config.ts, playwright.config.ts, tests/setup/supabase.ts, tests/setup/auth.ts, tests/unit/token.test.ts, tests/unit/i18n-keys.test.ts, tests/e2e/walking-skeleton.spec.ts, tests/e2e/anon-link-view.spec.ts, tests/e2e/oauth-upgrade.spec.ts, tests/e2e/token-revoke.spec.ts</files>
  <read_first>
    - .planning/phases/01-spine-plan-lifecycle/01-VALIDATION.md (Wave 0 requirements, sampling rate, mock-vs-real strategy)
    - .planning/phases/01-spine-plan-lifecycle/01-RESEARCH.md §Validation Architecture (test pyramid, framework versions), §Area 5 step 13 (walking-skeleton E2E example), §Open Question Q1 (CI OAuth bypass approach)
    - .planning/phases/01-spine-plan-lifecycle/01-UI-SPEC.md §Microcopy Catalog (es.json key coverage test references this file)
  </read_first>
  <action>
    Build the Wave 0 test scaffold. Every later task will reference one of these stub specs in its `<verify>` block.

    1) `vitest.config.ts` — Vitest 2.x config. Two test environments routed by file pattern: `node` for `tests/unit/**/*.test.ts` and `tests/integration/**/*.test.ts`; `jsdom` for any `*.tsx` test (none in Phase 1 but configure for future). Globals enabled. Test include pattern `tests/**/*.test.ts(x)`. Resolve alias `@/*` → `./src/*`. Coverage provider `v8`, output to `./coverage`, include `src/lib/**/*.ts`.

    2) `playwright.config.ts` — Playwright 1.49+ config. `webServer.command` = `pnpm dev`, port 3000, `reuseExistingServer: !process.env.CI`, timeout 120s. Two projects: `chromium-desktop` (default viewport 1280×720) and `webkit-mobile` (Playwright `devices['iPhone 12']`, 375×667 effective). `testDir` = `./tests/e2e`. Retry 0 locally, 1 in CI. Trace `on-first-retry`. Output `./playwright-report` and `./test-results`.

    3) `tests/setup/supabase.ts` — Helper that creates a service-role Supabase client against the local instance using `process.env.NEXT_PUBLIC_SUPABASE_URL` + `process.env.SUPABASE_SERVICE_ROLE_KEY` (loaded from `.env.local` via `dotenv` import at top). Exports `getTestServiceRoleClient()`. Documents that real local Supabase via `pnpm supabase start` is the test prerequisite (RESEARCH §Area 5 + §Validation §Mock-vs-Real).

    4) `tests/setup/auth.ts` — Test-only OAuth bypass helper per RESEARCH §Open Question Q1 option (a): Exports `signInAsTestUser(page, email)` that calls Supabase Admin API to mint a session for a seeded test user (email `test@groupcoordinator.local`) and sets `sb-access-token` + `sb-refresh-token` cookies on the Playwright `page` context. The seed user itself is created in Plan 01-02's `supabase/seed.sql`. Marks the file with a top comment: `// THIS HELPER IS TEST-ONLY. Never import from src/`.

    5) `tests/unit/token.test.ts` — Implement the three token-generation tests from RESEARCH §Validation §Unit Tests verbatim: exact 22-char length, no `[01IlO]` characters, 10,000-token uniqueness collision check. Uses `customAlphabet` from nanoid with alphabet `23456789abcdefghjkmnpqrstuvwxyz`.

    6) `tests/unit/i18n-keys.test.ts` — Confirms every key in `es.json` exists in `en.json` and `pt.json` (recursively). Confirms `es.json` contains specific anchor keys: `plan.view.empty_heading`, `plan.share_dialog.title`, `auth.google_button`, `errors.token_revoked`. Reads JSON files via `node:fs` synchronously. Walks objects, builds dot-path sets, asserts equality with `.toEqual`.

    7) `tests/e2e/walking-skeleton.spec.ts` — STUB for Plan 01-04 (and gates Plan 01-02 schema validation). For Phase 01-01, the spec MUST exist but is allowed to be `.skip` until Plan 01-02's seed lands the test plan. Implement a placeholder first test that loads `http://localhost:3000/` and asserts the page contains the tagline text (`Nunca más pierdas la reserva en el chat del grupo.`). A second `test.skip(...)` blocks out the full create-plan flow with a TODO comment referencing Plan 01-04.

    8) `tests/e2e/anon-link-view.spec.ts` — STUB; `test.describe.skip` block annotated `TODO(Plan 01-03): unskip after invite handler + plan view land`.

    9) `tests/e2e/oauth-upgrade.spec.ts` — STUB; `test.describe.skip` block annotated `TODO(Plan 01-05): unskip after OAuth callback wires plan_members upsert`.

    10) `tests/e2e/token-revoke.spec.ts` — STUB; `test.describe.skip` block annotated `TODO(Plan 01-06): unskip after settings page ships`.

    Each stub file must include a top-of-file comment block listing: which downstream plan owns un-skipping, which requirement IDs the spec covers, and which automated assertions belong inside.

    DO NOT install Playwright browsers in this task; the install runs in CI / the executor's environment. Document the command in `package.json` `scripts.postinstall` is forbidden (slows clone); instead add a comment in README-equivalent in `package.json` description. Actually keep package.json description per Next.js default; document the browser install command in `playwright.config.ts` header comment.

    11) Add a final smoke check: `pnpm test:unit` must exit 0 after the unit specs run. The Playwright stub specs are `.skip`'d (except walking-skeleton placeholder), so the e2e run with `pnpm test:e2e` should also exit 0 with passes (placeholder asserts text) + skipped descriptions.
  </action>
  <verify>
    <automated>pnpm test:unit --run 2>&1 | tail -20 && pnpm exec playwright install --with-deps chromium webkit 2>&1 | tail -5 && pnpm supabase start 2>&1 | tail -3 && (pnpm dev > /tmp/dev-server.log 2>&1 &) && sleep 8 && pnpm test:e2e --project=chromium-desktop tests/e2e/walking-skeleton.spec.ts 2>&1 | tail -10; pkill -f 'next dev' || true; pnpm supabase stop || true</automated>
  </verify>
  <acceptance_criteria>
    - `cat vitest.config.ts | grep -E 'jsdom|node'` matches both environments
    - `cat playwright.config.ts | grep -E "iPhone 12"` matches (webkit-mobile project configured)
    - `cat playwright.config.ts | grep -E "webServer"` includes `pnpm dev`
    - `ls tests/setup/{supabase,auth}.ts tests/unit/{token,i18n-keys}.test.ts tests/e2e/{walking-skeleton,anon-link-view,oauth-upgrade,token-revoke}.spec.ts` lists 8 files
    - `pnpm test:unit --run` exits 0 with at least 4 test assertions passing (token: 3, i18n-keys: ≥1)
    - `grep -E "test.skip|test.describe.skip" tests/e2e/{anon-link-view,oauth-upgrade,token-revoke}.spec.ts | wc -l` returns ≥ 3
    - `grep -E "TODO\(Plan 01-(0[3-6])\)" tests/e2e/*.spec.ts | wc -l` returns ≥ 3 (downstream un-skip owners annotated)
    - `grep -E "test\(|test\.skip\(" tests/e2e/walking-skeleton.spec.ts | wc -l` ≥ 1
    - `head -5 tests/setup/auth.ts | grep -i "test-only"` matches (safety comment present)
  </acceptance_criteria>
  <done>Wave 0 harness runs locally and in CI; unit suite exits 0; one E2E placeholder asserts marketing landing renders; three downstream-owned stubs are present and skip cleanly. VALIDATION.md frontmatter `wave_0_complete: true` should be set by the executor after this task lands.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Skeleton smoke test — human verifies dev stack runs end-to-end</name>
  <files>(none — manual verification task; no file modifications)</files>
  <action>Human verification only — execute the steps in <how-to-verify>.</action>
  <verify>Manual confirmation via developer (no automated command).</verify>
  <done>Developer types "approved" after dev stack runs.</done>
  <what-built>
    The bootstrap task assembles a deep stack (Next.js 15.5 + Supabase CLI + Drizzle Kit + shadcn + next-intl + Biome + Vitest + Playwright) that is hard to fully automate-verify in one go. This checkpoint pauses execution so the developer confirms the dev stack actually starts, the Spanish landing renders, and Supabase Studio is reachable. The next plan (01-02 schema + RLS) cannot proceed safely without this confirmation because every downstream task assumes a working bootstrap.
  </what-built>
  <how-to-verify>
    1. Open a fresh terminal and run `pnpm install` (already done by Task 1, just verify clean state)
    2. Run `pnpm supabase start` — expect output ending in "API URL: http://localhost:54321" and a Studio URL `http://localhost:54323`
    3. Copy `supabase status` output values into `.env.local` (use `.env.local.example` as template)
    4. Run `pnpm dev` — expect "Ready in" message and no errors
    5. Open `http://localhost:3000/` in a browser at 375px viewport (Chrome DevTools mobile emulation). Confirm: the wordmark "GroupCoordinator" displays centered, the tagline reads "Nunca más pierdas la reserva en el chat del grupo." in Spanish, the CTA button reads "Crear un plan gratis", and the footer reads "GroupCoordinator · 2026"
    6. Open `http://localhost:3000/en` — confirm same layout renders (English keys are stubbed copies, so Spanish text is acceptable here; the goal is verifying the `[locale]` segment routes work)
    7. Open `http://localhost:54323` (Supabase Studio) — confirm the dashboard loads and shows zero tables under `public` schema (schema lands in Plan 01-02)
    8. Run `pnpm test:unit --run` — confirm green
    9. Run `pnpm test:e2e --project=chromium-desktop tests/e2e/walking-skeleton.spec.ts` — confirm green (the placeholder assertion)

    No console errors expected at any step. If the landing page shows hydration warnings about `<html lang>`, that's a known next-intl + RSC interaction — note it but do NOT block; Plan 01-02 can re-verify.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Plan 01-02 schema work, or describe issues so they can be fixed before continuing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js | Public marketing page is the only surface in this plan; no user input crosses the boundary yet |
| Local dev host → Docker (Supabase) | Trusted; localhost-only binding via supabase CLI defaults |
| Repo files → Search engine crawlers | `robots.txt` Disallow /plan/ and /i/ (D-22) is the only crawler-facing decision in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `public/robots.txt` | mitigate | Disallow `/plan/` and `/i/` directories; verified via Task 1 acceptance criterion |
| T-01-02 | Information Disclosure | `.env.local.example` placeholder values committed | accept | Only example values committed; `.env.local` itself is gitignored; documented in Task 1 |
| T-01-03 | Tampering | Supabase CLI install / Docker image source | accept | Use official `supabase/tap/supabase` brew tap; documented in user_setup |
| T-01-SC | Tampering | npm/pnpm installs from package.json | accept | All packages are well-known (@supabase/*, drizzle-orm, next-intl, nanoid, react-hook-form, zod, lucide-react, sonner, vitest, @playwright/test, @biomejs/biome). Phase 1 RESEARCH did not surface a `## Package Legitimacy Audit` table; all packages are public-registry mainstream with multi-million weekly downloads. Treat as `[VERIFIED]` by exception — no `[ASSUMED]`/`[SUS]` packages introduced; if RESEARCH later adds an audit, re-run the gate. |

</threat_model>

<verification>
After Task 3 approval:
- `pnpm install && pnpm check && pnpm build && pnpm test:unit --run` exits 0 end-to-end
- Bare `http://localhost:3000/` renders the Spanish marketing skeleton at 375px without horizontal scroll
- All 17 shadcn UI components present in `src/components/ui/`
- `01-VALIDATION.md` frontmatter updated to `wave_0_complete: true`
</verification>

<success_criteria>
- Repository builds, lints, and type-checks clean
- Marketing landing (Surface 8) renders in Spanish with all four UI-SPEC elements (logo, tagline, CTA, footer)
- Local Supabase stack reachable on documented ports
- Wave 0 test harness present; unit suite green; one E2E placeholder green; three downstream stubs skip cleanly
- No hardcoded Spanish strings outside `src/lib/i18n/messages/` (allowlist: `src/app/global-error.tsx`)
- robots.txt locks `/plan/` and `/i/`
- Human checkpoint passed (Task 3)
</success_criteria>

<output>
Create `.planning/phases/01-spine-plan-lifecycle/01-01-SUMMARY.md` when done. Include:
- Exact dependency versions installed (from `pnpm list --depth=0`)
- Supabase local connection-string template (placeholders only)
- Wave 0 test counts (unit + skipped E2E)
- Any deviations from RESEARCH §Area 5 bootstrap sequence and why
- Confirmation that the Custom Access Token Hook function is NOT yet created (Plan 01-02 owns it) but the `supabase/config.toml` registers the hook URI
</output>
