---
phase: 01-spine-plan-lifecycle
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/app/[locale]/(app)/plan/new/page.tsx
  - src/components/plan/CreatePlanForm.tsx
  - src/components/plan/ShareDialog.tsx
  - src/components/plan/ShareDialogTrigger.tsx
  - src/components/plan/PlanHeader.tsx
  - src/server/actions/plan.ts
  - src/server/actions/invite-token.ts
  - src/app/api/og/[plan_slug]/route.tsx
  - src/lib/og/fonts.ts
  - src/lib/auth/require-user.ts
  - src/app/[locale]/(app)/plan/[slug]/page.tsx
  - tests/integration/create-plan.test.ts
  - tests/integration/og-image.test.ts
  - tests/e2e/create-plan.spec.ts
autonomous: false
requirements:
  - PLAN-01
  - PLAN-02
  - AUTH-06
truths:
  - "Authenticated user can access /plan/new; anonymous + unauthenticated visits redirect to /auth/sign-in?next=/plan/new (AUTH-06)"
  - "createPlan Server Action requires only title (D-06, PLAN-01); start_date, end_date, description are optional; Zod validates server-side AND client-side; title is required with the exact es.json error key"
  - "createPlan inserts a plans row (owner_id = auth.uid()) AND a default invite_token row (role='viewer'), in a single transaction; redirects to /plan/[slug]?share=1"
  - "/plan/[slug]?share=1 auto-opens the share dialog (Surface 2) on first render; dialog displays the /i/[token] link in font-mono, a Copy button (icon-only with aria-label), and the Web Share API CTA (visible only when navigator.share exists)"
  - "GET /api/og/[plan_slug] returns a 1200×630 PNG via next/og rendering the emerald-700 → emerald-900 gradient + plan title (Geist Sans bold 56px) + 'Creado por {name}' + date range; cached at edge (D-09)"
  - "<meta property='og:image' content='/api/og/[plan_slug]'> wired on /plan/[slug] and /i/[token]; share link displayed in dialog is `${NEXT_PUBLIC_SITE_URL}/i/[token]`"
  - "After create, plan header (Surface 4 owner state) shows the plan title (160px truncate @ 375px) + member stack + settings gear icon visible to owner only (D-14 affordance + UI-SPEC §Surface 4)"
  - "No hardcoded Spanish strings; all microcopy via es.json keys (D-20)"
must_haves:
  truths:
    - "Authenticated user creates plan with title only and gets a shareable /i/[token] link in under 30 seconds (PLAN-01, PLAN-02, ROADMAP success criterion 2)"
    - "Share dialog renders with Web Share API + Copy fallback per Surface 2; copy success replaces icon with Check for 2s (no toast, per Surface 2 spec)"
    - "OG preview is the marketing surface (MP-5) — visually correct gradient + title + creator at 1200×630"
  artifacts:
    - path: src/server/actions/plan.ts
      provides: "createPlan(formData), updatePlan(planId, formData), archivePlan(planId) Server Actions; all guarded by require-user; use RLS-bound Supabase server client"
      exports: ["createPlan", "updatePlan", "archivePlan"]
    - path: src/server/actions/invite-token.ts
      provides: "mintInviteToken(planId, role), revokeInviteToken(tokenId); server-action; uses RLS (owner-only INSERT/UPDATE per Plan 01-02 policies)"
      exports: ["mintInviteToken", "revokeInviteToken"]
    - path: src/components/plan/CreatePlanForm.tsx
      provides: "react-hook-form + Zod-resolved form per Surface 1 anatomy; title required, collapsible 'Más detalles' for dates + description"
    - path: src/components/plan/ShareDialog.tsx
      provides: "Surface 2: link block (font-mono bg-zinc-100 p-3 rounded-md truncate), Copy button (icon-only), Web Share CTA, Copiar link fallback, 'Ir al plan' close action"
    - path: src/app/api/og/[plan_slug]/route.tsx
      provides: "Dynamic OG image via next/og — gradient + title + creator + date range + wordmark (D-09)"
      exports: ["GET"]
    - path: src/lib/auth/require-user.ts
      provides: "Server-side guard: getRequiredUser(cookieStore) returns user or redirects to /auth/sign-in?next=…"
      exports: ["getRequiredUser"]
  key_links:
    - from: src/server/actions/plan.ts
      to: drizzle/db.ts
      via: db.insert(plans).values(...).returning() inside Server Action (RLS-bypassed since Server Action uses service-role for write WITH explicit owner_id = user.id from server-validated session — OR use the RLS-bound Supabase server client which enforces plans_insert_authenticated policy)
      pattern: "db.insert\\(plans\\)"
    - from: src/app/[locale]/(app)/plan/new/page.tsx
      to: src/lib/auth/require-user.ts
      via: getRequiredUser redirects to /auth/sign-in if no session
      pattern: "getRequiredUser"
    - from: src/app/[locale]/(app)/plan/[slug]/page.tsx
      to: src/components/plan/ShareDialog.tsx
      via: ?share=1 query param triggers initial open state passed to client island
      pattern: "share=1"
    - from: src/app/api/og/[plan_slug]/route.tsx
      to: src/lib/og/fonts.ts
      via: fetch base64-embedded Geist Sans for Satori
      pattern: "ImageResponse"
user_setup:
  - service: google-oauth-local
    why: "Local Supabase Studio must have Google OAuth provider enabled before /plan/new can be reached via real OAuth (Plan 01-05 wires the callback; this plan only needs the provider toggle so the require-user redirect-loop works in dev)"
    env_vars:
      - name: GOOGLE_OAUTH_CLIENT_ID
        source: "Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID (Web application)"
      - name: GOOGLE_OAUTH_CLIENT_SECRET
        source: "Same OAuth 2.0 Client → Client secret"
    dashboard_config:
      - task: "Create Google Cloud OAuth 2.0 Web application with authorized redirect URI http://localhost:54321/auth/v1/callback and http://localhost:3000/auth/callback; add the client id/secret to supabase/config.toml under [auth.external.google]"
        location: "Google Cloud Console + Supabase config"
      - task: "Enable Google provider in local Supabase Studio Authentication → Providers"
        location: "http://localhost:54323 → Authentication → Providers"
---

<objective>
Deliver the authenticated plan-creation vertical slice: form → Server Action → DB write → share dialog → invite link displayed → OG preview rendered. After this plan, a signed-in user creates a plan with just a title and gets a shareable `/i/[token]` link in well under 30 seconds (PLAN-01 + PLAN-02 + AUTH-06 + the wedge metric "setup en 30 segundos").

Purpose: Completes the OWNER side of the create-share-view loop. Plan 01-03 already shipped the guest side; this plan ships the owner side, which means a real human can now exercise the full Phase 1 wedge: create a plan → share the link → guest opens it → guest sees the plan. The Walking Skeleton becomes user-demonstrable.

Output: One create page + one create form + one share dialog + two Server Actions + one OG image route + one require-user guard + plan-page integration of the share dialog + 3 test files.

User story (MVP_MODE): **As a** signed-in plan organizer, **I want to** create a plan with just a title and get a shareable invite link auto-opened in a dialog, **so that** I can paste the link into WhatsApp within 30 seconds of having the idea.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-spine-plan-lifecycle/01-CONTEXT.md
@.planning/phases/01-spine-plan-lifecycle/01-RESEARCH.md
@.planning/phases/01-spine-plan-lifecycle/01-UI-SPEC.md
@.planning/phases/01-spine-plan-lifecycle/01-SKELETON.md
@.planning/phases/01-spine-plan-lifecycle/01-03-SUMMARY.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Surfaces and contracts from Plans 01-01 → 01-03 this plan consumes: -->

Surface 1 (UI-SPEC §Surface 1 — Create Plan Form):
- Page title 'Nuevo plan' (text-display 24px)
- Form card (p-4 rounded-lg shadow-sm); Título * field focused on mount
- Collapsible "Agregar fechas y descripción" with chevron-right rotating 90° when open
- Fecha de inicio / Fecha de fin native date inputs; Descripción textarea (3 rows, resize-none)
- CTA "Crear plan" full-width emerald
- Inline error "El título del plan es obligatorio." (key: plan.create.field_title_required)
- Server error banner at form top (key: plan.create.error_server)

Surface 2 (UI-SPEC §Surface 2 — Post-Create Share Dialog):
- Dialog max-w 480px, rounded-lg, p-6
- Title 'plan.share_dialog.title' (text-heading 20px semibold)
- Link block: font-mono text-sm bg-zinc-100 p-3 rounded-md truncate
- Icon-only Copy button 36×36 aria-label='Copiar link'; toggles to Check icon for 2s on success (NO toast)
- Primary CTA 'plan.share_dialog.share_via' (Web Share API; visible only if navigator.share exists)
- Secondary CTA 'plan.share_dialog.copy_link' (always visible as fallback)
- Per-channel hint 'plan.share_dialog.channel_hint' (text-small zinc-500, mt-2)
- Divider + "Ir al plan" text-link footer action

Surface 4 (UI-SPEC §Surface 4 — Authenticated Plan View) additions to deliver in this plan:
- Header shows plan title (truncated 160px @ 375px), avatar stack, settings gear icon visible only when user.id === plan.owner_id
- Sign-in affordance bar HIDDEN (not just CSS-hidden — removed from DOM)
- Avatar dropdown-menu with "Mis planes" → /me, "Cerrar sesión" → sign-out action, "Configuración del plan" → /plan/[slug]/settings (owner only)

OG image (UI-SPEC §OG Share Preview + D-09):
- 1200×630 PNG
- Linear gradient emerald-700 (#047857) → emerald-900 (#064e3b), left→right
- Plan title Geist Sans bold 56px white, 2-line max
- 'Creado por {name}' Geist Sans regular 28px rgba(255,255,255,0.8)
- Date range Geist Sans regular 24px rgba(255,255,255,0.7) if set
- Wordmark 'GroupCoordinator' bottom-right Geist Sans semibold 20px rgba(255,255,255,0.6)
- Geist Sans fetched once and embedded (base64) or fetched from @vercel/fonts

From Plan 01-02:
- src/lib/auth/invite-token.ts — generateToken, generateSlug
- src/lib/validation/plan.ts — createPlanSchema, revokeTokenSchema
- drizzle/schema.ts — plans, invite_tokens tables
- src/lib/supabase/server.ts — createServerClient

From Plan 01-03:
- src/components/plan/PlanHeader.tsx — currently does NOT render the plan title (Plan 01-03 left this as a TODO); this plan completes it
- src/app/[locale]/(app)/plan/[slug]/page.tsx — adds the ShareDialog mount when ?share=1
- /auth/sign-in route does NOT yet exist — Plan 01-05 builds it. For this plan, the require-user redirect target is `/auth/sign-in?next=…`; visiting that URL before Plan 01-05 lands shows 404. Document this in code as a TODO.

es.json keys consumed (all already present from Plan 01-01):
- plan.create.* (page_title, field_title_label, field_title_placeholder, field_title_required, collapsible_trigger, field_start_date_label, field_end_date_label, field_dates_helper, field_description_label, field_description_placeholder, field_description_helper, submit, error_server)
- plan.share_dialog.* (title, share_via, copy_link, channel_hint, go_to_plan)
- common.copied (aria-live announcement)
- nav.plan_settings (header dropdown for owner)
- og.plan_created_by, og.wordmark
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: require-user guard + plan create page + form + Server Actions</name>
  <files>src/lib/auth/require-user.ts, src/app/[locale]/(app)/plan/new/page.tsx, src/components/plan/CreatePlanForm.tsx, src/server/actions/plan.ts, src/server/actions/invite-token.ts, tests/integration/create-plan.test.ts</files>
  <read_first>
    - .planning/phases/01-spine-plan-lifecycle/01-RESEARCH.md §Area 5 step 12 (createPlan Server Action exact code), §Open Question 2 (use_count atomicity — note for invite-token mint)
    - .planning/phases/01-spine-plan-lifecycle/01-CONTEXT.md (D-06 title-only required, D-07 auto-open share dialog, D-22 noindex)
    - .planning/phases/01-spine-plan-lifecycle/01-UI-SPEC.md §Surface 1 (full anatomy + accessibility + responsive)
    - src/lib/validation/plan.ts, src/lib/auth/invite-token.ts, drizzle/schema.ts (Plan 01-02 contracts)
    - src/components/ui/{form,input,label,button,select,textarea}.tsx (Plan 01-01)
  </read_first>
  <action>
    1) `src/lib/auth/require-user.ts` — exports `async function getRequiredUser(cookieStore: ReadonlyRequestCookies, nextPath: string): Promise<User>`:
       - Creates server Supabase client via `createServerClient(cookieStore)`
       - Calls `supabase.auth.getUser()`
       - If `!user` OR `user.is_anonymous === true` → calls `redirect('/auth/sign-in?next=' + encodeURIComponent(nextPath))` (Next.js `redirect` from `next/navigation`)
       - Otherwise returns the user
       Top comment: `// AUTH-06 enforcement: every mutation surface uses this guard`.

    2) `src/app/[locale]/(app)/plan/new/page.tsx` — RSC. Calls `setRequestLocale(locale)`, then `const user = await getRequiredUser(await cookies(), '/plan/new')`. Renders:
       - `<h1 className="text-2xl font-semibold text-zinc-950 px-4 pt-6">{t('plan.create.page_title')}</h1>`
       - Container max-w-[480px] mx-auto on md+, full-width on mobile, px-4
       - `<CreatePlanForm />` (client island)
       - `export const metadata = { robots: 'noindex' }` and `title: t('plan.create.page_title')` (via generateMetadata async)

    3) `src/components/plan/CreatePlanForm.tsx` — `'use client'`. Uses react-hook-form with `zodResolver(createPlanSchema)`. Anatomy mirrors UI-SPEC §Surface 1:
       - Title input: `<Input>` with placeholder from `t('plan.create.field_title_placeholder')`, autoFocus on mount; inline error below with `role="alert"` (announce via aria-live)
       - Collapsible trigger: button with chevron-right icon (lucide `ChevronRight`) that rotates 90° on open; copy `t('plan.create.collapsible_trigger')`; min-height 44px
       - Inside collapsible: Fecha de inicio (input type="date"), Fecha de fin (input type="date"), Descripción (textarea, 3 rows, resize-none). All labels + helpers via es.json keys
       - Submit button full-width emerald 'Crear plan' (`t('plan.create.submit')`); `aria-busy="true"` when submitting
       - On submit: calls `createPlan` Server Action with FormData. Server Action returns `{ error }` on failure or redirects on success. If `{ error }`, set form-level error state and render banner above fields with `t('plan.create.error_server')` (NOT the Zod field error — the field error is already inline)
       - Accessibility: `<form aria-label="Crear nuevo plan">`, `<fieldset>` for collapsible with `<legend>` "Detalles opcionales" (key `plan.create.collapsible_trigger` reused, or add a new key if needed)

    4) `src/server/actions/plan.ts` — `'use server'`. Three actions:
       - `createPlan(formData: FormData)`:
         a. Parse with `createPlanSchema` → on fail, return `{ error: parsed.error.flatten() }`
         b. `const cookieStore = await cookies(); const supabase = createServerClient(cookieStore); const { data: { user } } = await supabase.auth.getUser()` — if no user, `redirect('/auth/sign-in?next=/plan/new')`
         c. Use the RLS-bound Supabase client (`from('plans').insert(...).select().single()`) so the plans_insert_authenticated RLS policy enforces `owner_id = auth.uid()`. Pass `slug: generateSlug(), title, description ?? null, start_date ?? null, end_date ?? null, owner_id: user.id` — RLS WITH CHECK enforces owner_id matches caller
         d. After plan insert, INSERT into plan_members (`plan_id, user_id: user.id, role: 'owner', joined_at: now()`) so the owner has a membership row (Plan 01-02 RLS allows this; D-13 specifies role: 'owner')
         e. Call `mintInviteToken(plan.id, 'viewer')` — server-internal helper from invite-token.ts; returns the new token row
         f. `redirect(`/plan/${plan.slug}?share=1`)` — the ?share=1 query param triggers Surface 2 auto-open
       - `updatePlan(planId, formData)` — used by Plan 01-06; for this plan, write the stub function with comment `// implemented in Plan 01-06 — not yet implemented`; the stub must `throw new Error('updatePlan: not yet implemented — Plan 01-06')` if called
       - `archivePlan(planId)` — same stub treatment with comment `// implemented in Plan 01-06 — not yet implemented` and `throw new Error('archivePlan: not yet implemented — Plan 01-06')`

    5) `src/server/actions/invite-token.ts` — `'use server'`. Two actions:
       - `mintInviteToken(planId: string, role: 'viewer' | 'editor' = 'viewer'): Promise<{ id: string; token: string; role: string }>`:
         a. Auth-check via getRequiredUser
         b. Generate token via `generateToken()`
         c. INSERT into invite_tokens via RLS-bound supabase client (`role`, `created_by: user.id`); RLS policy invite_tokens_insert_owner enforces ownership
         d. Return `{ id, token, role }`
       - `revokeInviteToken(tokenId: string)` — Plan 01-06 stub for now; throw "not yet implemented".

    6) `tests/integration/create-plan.test.ts` — uses test-bypass auth (Plan 01-01's `tests/setup/auth.ts`) to mint a session for `test@groupcoordinator.local`. Then:
       - Calls `createPlan` Server Action directly with a constructed FormData (`title: 'Plan de integración'`)
       - Asserts the action throws a `NEXT_REDIRECT` error with destination `/plan/[slug]?share=1` (Server Action redirects raise a special error in Next 15.5 testing — catch and inspect the digest)
       - Queries the DB to confirm: plans row exists with slug matching the redirect target, plan_members row exists with role='owner' for the test user, invite_tokens row exists with role='viewer'
       - Asserts an empty-title submission returns `{ error }` with the field error keyed to `title`
       - Cleans up: deletes the created plan via service-role at the end
       Test must skip if local Supabase + auth hook are not running.

    Constraints:
    - DO NOT use service-role for the plans/invite_tokens INSERT in createPlan — RLS must be the security source of truth (Plan 01-02 policies enforce ownership; service-role is for routes that genuinely cannot use RLS, like /api/invite/[token])
    - DO NOT log the user's email or any PII (D-21 / MP-3)
    - DO NOT hardcode Spanish — all strings via es.json
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm check && pnpm test:unit --run tests/integration/create-plan.test.ts 2>&1 | tail -20 && grep -E "getRequiredUser" src/lib/auth/require-user.ts && grep -E "redirect\(.*share=1" src/server/actions/plan.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit` exits 0
    - `pnpm check` exits 0
    - `pnpm test:unit --run tests/integration/create-plan.test.ts` exits 0 with ≥ 4 assertions
    - `grep -c "redirect.*share=1" src/server/actions/plan.ts` returns ≥ 1
    - `grep -c "createPlanSchema" src/server/actions/plan.ts` returns ≥ 1 (server-side Zod re-validation)
    - `grep -c "use server" src/server/actions/plan.ts` returns 1
    - `grep -c "use server" src/server/actions/invite-token.ts` returns 1
    - `grep -c "use client" src/components/plan/CreatePlanForm.tsx` returns 1
    - `grep -c "use client" src/app/[locale]/[(]app[)]/plan/new/page.tsx` returns 0 (RSC)
    - `grep -c "getRequiredUser" src/app/[locale]/[(]app[)]/plan/new/page.tsx` returns ≥ 1
    - `grep -rE "\"[A-ZÁÉÍÓÚÑa-záéíóúñ]{4,}\"" src/components/plan/CreatePlanForm.tsx src/app/[locale]/[(]app[)]/plan/new src/server/actions | grep -v 'src/lib/i18n' | grep -v -E '^[^:]+:\s*//' | wc -l` returns 0
    - `grep -E "role: ?.owner" src/server/actions/plan.ts` matches (owner plan_members insert)
    - `grep -E "createServerClient|service-role" src/server/actions/plan.ts | grep -c "service-role"` returns 0 (no service-role in plan.ts)
    - `grep -cE "Plan 01-06|not yet implemented" src/server/actions/plan.ts` returns ≥ 2 (confirms updatePlan + archivePlan stubs are properly annotated as deferred to Plan 01-06)
  </acceptance_criteria>
  <done>Authenticated users can POST /plan/new form and land on /plan/[slug]?share=1 with the plan, owner-membership, and a default viewer invite_token all created in one Server Action. RLS policies enforce ownership; no service-role used in the create path.</done>
</task>

<task type="auto">
  <name>Task 2: Share dialog (Surface 2) + plan header completion + OG image route</name>
  <files>src/components/plan/ShareDialog.tsx, src/components/plan/ShareDialogTrigger.tsx, src/components/plan/PlanHeader.tsx, src/app/[locale]/(app)/plan/[slug]/page.tsx, src/app/api/og/[plan_slug]/route.tsx, src/lib/og/fonts.ts, tests/integration/og-image.test.ts, tests/e2e/create-plan.spec.ts</files>
  <read_first>
    - .planning/phases/01-spine-plan-lifecycle/01-UI-SPEC.md §Surface 2 (full anatomy + Web Share API fallback rule), §Surface 4 (authenticated header — gear icon owner-only, hide sign-in bar from DOM), §OG Share Preview (canvas composition + font loading)
    - .planning/phases/01-spine-plan-lifecycle/01-CONTEXT.md (D-07 auto-open dialog, D-09 dynamic OG, D-14 post-OAuth header state)
    - src/components/plan/PlanHeader.tsx (Plan 01-03 — currently does NOT render plan title; this task completes it)
    - src/components/ui/dialog.tsx, src/components/ui/dropdown-menu.tsx, src/components/ui/avatar.tsx (Plan 01-01)
  </read_first>
  <action>
    1) `src/components/plan/ShareDialog.tsx` — `'use client'`. Props `{ open: boolean; onOpenChange: (open: boolean) => void; inviteUrl: string; planTitle: string; slug: string }`. Uses shadcn `<Dialog>`:
       - Title: `t('plan.share_dialog.title')` with `text-xl font-semibold text-zinc-950`
       - Link block: `<div className="font-mono text-sm bg-zinc-100 rounded-md p-3 truncate">{inviteUrl}</div>` paired with an icon-only Copy button (36×36, lucide `Copy`/`Check` toggle, `aria-label={t('plan.share_dialog.copy_link')}`). On click: `await navigator.clipboard.writeText(inviteUrl)`, set local state to show Check icon for 2 seconds, fire an aria-live polite announcement with text `t('common.copied')` (visually hidden `<span role="status" className="sr-only">`)
       - Web Share API CTA (primary, full-width emerald, 52px h, rounded-md): visible only when `typeof navigator !== 'undefined' && 'share' in navigator`; copy `t('plan.share_dialog.share_via')` with `Share2` icon; onclick `navigator.share({ title: planTitle, url: inviteUrl })` wrapped in try/catch (user-cancel throws)
       - Copy link fallback CTA (always visible — full-width when Web Share absent; secondary outline when Web Share present): copy `t('plan.share_dialog.copy_link')`
       - Channel hint paragraph: `text-sm text-zinc-500 mt-2` with `t('plan.share_dialog.channel_hint')`
       - Divider + footer text-link 'Ir al plan' (`t('plan.share_dialog.go_to_plan')`) that calls `onOpenChange(false)` (user is already on plan page; just closes dialog)
       - max-w-[480px] mobile / 512px desktop, rounded-lg, p-6, shadow-md
       - Focus management: first focus on dialog open is the Copy button (shadcn default focuses first interactive; the link block isn't interactive)

    2) `src/components/plan/ShareDialogTrigger.tsx` — `'use client'`. Props `{ planTitle: string; slug: string; inviteToken: string; openOnMount?: boolean }`. Manages dialog open state. If `openOnMount === true` (from `?share=1`), opens dialog after render. Renders `<ShareDialog>` with `inviteUrl = ${NEXT_PUBLIC_SITE_URL}/i/${inviteToken}` (use `process.env.NEXT_PUBLIC_SITE_URL` at build time, fall back to `window.location.origin` at runtime). Also exposes a "Compartir" button (lucide `Share2` icon) in the plan header for re-opening the dialog later. NOTE: in this plan, the trigger button is added to the plan header (Surface 4 chrome).

    3) `src/components/plan/PlanHeader.tsx` — UPDATE the Plan 01-03 placeholder. Add:
       - Plan title slot: when `plan` prop is non-null, render `<h2 className="text-xl font-semibold text-zinc-950 truncate max-w-[160px] md:max-w-[240px] lg:max-w-[400px]">{plan.title}</h2>` to the right of the Logo
       - Member avatar stack: top 3 members @ 375px (5 @ md+, all @ lg+); each 28px `rounded-full`, overlapping by 8px; +N overflow pill when more than the cap
       - For authenticated owner: render a settings gear icon link `<Link href={`/plan/${plan.slug}/settings`} aria-label={t('nav.plan_settings')}>` with `Settings` icon (16px) and a 44×44px tap target; visible only when `user?.id === plan.owner_id`
       - For authenticated non-owner: same chrome WITHOUT the gear icon
       - For anonymous: existing "Iniciar sesión" link (Plan 01-03)
       - For authenticated (owner or non-owner): replace "Iniciar sesión" with avatar (32px rounded-full) + chevron-down → `<DropdownMenu>` containing `Mis planes` → /me, `Cerrar sesión` → calls supabase.auth.signOut() then redirects to /, and for owner: `Configuración del plan` → /plan/[slug]/settings (D-14)
       - Mark as `'use client'` ONLY for the dropdown interaction; consider splitting into a server `<PlanHeader>` shell + client `<HeaderUserMenu>` island. Pragmatic for Phase 1: mark the whole component `'use client'` and accept the hydration cost; document the split as a Phase 7 optimization

    4) UPDATE `src/app/[locale]/(app)/plan/[slug]/page.tsx`:
       - Add parsing of `searchParams` for `share=1`
       - If owner AND `share=1` → fetch the most-recent invite_tokens row for this plan (`from('invite_tokens').select('token').eq('plan_id', plan.id).order('created_at', { ascending: false }).limit(1).single()`) and pass to a `<ShareDialogTrigger openOnMount={true} ... />` mount
       - If authenticated (owner OR non-owner anon): hide the SignInAffordanceBar from the rendered tree (NOT just CSS-hidden — conditional render)
       - Pass `user` and `plan` to `<PlanHeader>` so it can render the owner gear, member stack, and auth state

    5) `src/lib/og/fonts.ts` — exports `async function loadGeistSans(): Promise<{ regular: ArrayBuffer; semibold: ArrayBuffer; bold: ArrayBuffer }>`. Fetches from a reliable source: prefer base64 embedding from `node_modules/geist/dist/fonts/geist-sans/*.woff2` at build time. Fallback: fetch from `https://github.com/vercel/geist-font/raw/main/packages/next/dist/fonts/geist-sans/Geist-{Regular,SemiBold,Bold}.woff2` at first request and cache in-memory. Document the choice in a top comment.

    6) `src/app/api/og/[plan_slug]/route.tsx` — `export const runtime = 'edge'`. `GET(request, { params })`:
       - `const { plan_slug } = await params`
       - Fetch plan via SERVICE-ROLE client (OG image must work even for unauthenticated requests — it IS the marketing surface): `from('plans').select('title, start_date, end_date, owner_id, plan_members!inner(user_id)').eq('slug', plan_slug).eq('archived_at', null).maybeSingle()` — wait: cleaner approach: just fetch plan title + dates by slug, and the owner's display name via a join with auth.users. Use service-role to bypass RLS (the OG image is intentionally public)
       - Fetch creator name via `admin.auth.admin.getUserById(plan.owner_id)` → `data.user.user_metadata.full_name` (or fallback to email prefix)
       - If plan not found OR archived_at not null → return 404 PNG (simple 1200×630 gradient with "Plan no disponible" text; reuse same template)
       - Construct `ImageResponse` from `next/og`. Per UI-SPEC §OG Share Preview:
         * Container: 1200×630, linear-gradient(to right, #047857, #064e3b)
         * Centered content zone max-width 900px
         * Plan title: Geist Sans bold 56px white, line-height 1.2, max 2 lines (truncate ~50 chars)
         * 'Creado por {name}': Geist Sans regular 28px rgba(255,255,255,0.8), mt-16 (16 in tailwind = 64px; for inline JSX style, use marginTop: 64)
         * Date range if set: 24px regular rgba(255,255,255,0.7)
         * Wordmark bottom-right: 'GroupCoordinator' Geist Sans semibold 20px rgba(255,255,255,0.6)
       - Load fonts via `loadGeistSans()`; pass to `ImageResponse({ fonts: [{ name: 'Geist', data: ..., weight: 400 }, { ... weight: 600 }, { ... weight: 700 }] })`
       - Cache headers: `'Cache-Control': 'public, max-age=300, s-maxage=3600, stale-while-revalidate=86400'` — short edge cache so a title change becomes visible within an hour without forcing a CDN purge

    7) UPDATE the `/plan/[slug]/page.tsx` metadata to include `openGraph: { images: [{ url: `${siteUrl}/api/og/${slug}`, width: 1200, height: 630 }] }, twitter: { card: 'summary_large_image', images: [...same...] }`. Same for `/i/[token]` — but the invite handler 302s before any HTML is rendered, so the OG metadata lives on /plan/[slug] only. When users paste the share link in WhatsApp, WhatsApp follows the 302 and reads the OG meta from the /plan/[slug] response. Confirm via the Validation manual check.

    8) `tests/integration/og-image.test.ts` — Vitest. Imports the GET handler from the route file and calls it with a constructed NextRequest pointing to `/api/og/seed-plan`. Asserts:
       - Status 200
       - Content-Type starts with `image/png`
       - Body byte length > 50000 (a real 1200×630 PNG with gradient + 56px title text is typically 80–150 KB; a blank/empty PNG would be < 10 KB — this threshold catches font-load failures and empty renders)
       - Cache-Control header present and includes `s-maxage`
       - `loadGeistSans()` resolves successfully when invoked before the request (assert no thrown error; the test imports `loadGeistSans` and awaits it once before the request)
       Second test for nonexistent slug → status 200 with "Plan no disponible" fallback. Assertions for the fallback path:
       - Body byte length > 50000 (same threshold — the fallback PNG still has gradient + text)
       - **Cache-Control header present and includes `s-maxage`** (fallback path MUST also be edge-cacheable to prevent enumeration probes from consuming Vercel function budget)
       Visual regression of the rendered image is deferred per VALIDATION.md manual verification.

    9) `tests/e2e/create-plan.spec.ts` — Playwright. Uses test-auth bypass (sets `sb-access-token` for `test@groupcoordinator.local`). Steps:
       - Goto `/plan/new`
       - Fill title input with "Plan E2E"
       - Click submit
       - Assert URL matches `/plan/*\\?share=1`
       - Assert dialog with role="dialog" is visible
       - Assert the dialog contains a text that matches `^[a-z]+://[^/]+/i/[a-z0-9]{22}$` (the invite link)
       - Click "Copiar link" button (or detect Web Share button if browser exposes navigator.share — in Playwright Chromium, navigator.share exists ONLY when run in a top-level frame on https; in tests we expect the Copy button to be primary)
       - Assert clipboard content (Playwright `context.grantPermissions(['clipboard-read'])` and read clipboard) matches the same regex
       - Close dialog via "Ir al plan", assert no dialog visible, assert URL no longer has `?share=1` (or accept that ?share=1 persists — confirm Surface 2 spec: "Dialog closes, user lands on plan view (already on the plan route)" — the URL CAN keep ?share=1; the dialog's open state is local; do not assert the param removal)
       Cleanup: delete the created plan via service-role at end of test.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm check && pnpm build 2>&1 | tail -15 && pnpm supabase start 2>&1 | tail -2 && pnpm test:unit --run tests/integration/og-image.test.ts 2>&1 | tail -10 && pnpm test:e2e --project=chromium-desktop tests/e2e/create-plan.spec.ts 2>&1 | tail -15 && pnpm test:e2e --project=webkit-mobile tests/e2e/create-plan.spec.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit` exits 0
    - `pnpm build` exits 0
    - `pnpm test:e2e tests/e2e/create-plan.spec.ts` exits 0 on chromium-desktop AND webkit-mobile
    - `pnpm test:unit --run tests/integration/og-image.test.ts` exits 0
    - `curl -sI http://localhost:3000/api/og/seed-plan | grep -iE "content-type: image/png"` matches when dev server is up
    - `grep -c "navigator.share" src/components/plan/ShareDialog.tsx` returns ≥ 1
    - `grep -c "navigator.clipboard.writeText" src/components/plan/ShareDialog.tsx` returns ≥ 1
    - `grep -c "use client" src/components/plan/ShareDialog.tsx` returns 1
    - `grep -E "owner_id" src/components/plan/PlanHeader.tsx` matches (owner gate)
    - `grep -E "aria-label" src/components/plan/ShareDialog.tsx | wc -l` ≥ 1 (Copy button labeled)
    - `grep -E "ImageResponse|next/og" "src/app/api/og/[plan_slug]/route.tsx"` matches
    - `grep -E "Cache-Control.*s-maxage" "src/app/api/og/[plan_slug]/route.tsx"` matches
    - `grep -rE "\"[A-ZÁÉÍÓÚÑa-záéíóúñ]{4,}\"" src/components/plan/ShareDialog.tsx src/components/plan/PlanHeader.tsx | grep -v 'src/lib/i18n' | grep -v -E '^[^:]+:\s*//' | wc -l` returns 0
    - The OG metadata wired on /plan/[slug]/page.tsx: `grep -E "openGraph" src/app/[locale]/[(]app[)]/plan/[\\[]slug[\\]]/page.tsx` matches
  </acceptance_criteria>
  <done>Owner creates a plan, lands on /plan/[slug]?share=1 with Surface 2 dialog open, copies an /i/[token] link, and can navigate the plan header (Surface 4) with member stack + owner gear icon. /api/og/[plan_slug] renders a real PNG with the gradient + title + creator. E2E + integration tests prove it on Chromium desktop AND WebKit mobile 375×667.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual verification — share link works in WhatsApp + OG preview renders correctly</name>
  <files>(none — manual verification task; no file modifications)</files>
  <action>Human visual verification only — Claude cannot drive a WhatsApp client.</action>
  <verify>Manual: paste /i/[token] into WhatsApp and screenshot the rendered preview.</verify>
  <done>OG preview gradient + title + creator visible in WhatsApp + iMessage previews.</done>
  <what-built>
    The OG preview is "the marketing surface" (D-09 + CONTEXT.md specifics). It's the moment when a recipient sees the plan in WhatsApp/iMessage BEFORE clicking. There is no reliable way to automate "WhatsApp renders this OG image correctly" — WhatsApp's link-preview crawler is not exposed and visual quality is subjective. This checkpoint exists per VALIDATION.md §Manual-Only Verifications row 1 ("OG preview renders correctly in WhatsApp / iMessage").
  </what-built>
  <how-to-verify>
    1. Make sure local Supabase is running and the dev server is up at `http://localhost:3000`
    2. Manually expose your dev server to the public internet (one option: `pnpm dlx ngrok http 3000` — note the public URL it issues)
    3. Update `.env.local` `NEXT_PUBLIC_SITE_URL` to the ngrok URL and restart `pnpm dev`
    4. Sign in as the test owner (use the Supabase Studio "Authentication" tab to copy a magic link, OR sign in with Google if you've configured the OAuth provider per `user_setup`)
    5. Create a plan with the title "Despedida de Carlos en Cádiz" and set a date range (e.g. 15-20 jun 2026)
    6. Copy the `/i/[token]` link from the auto-opened share dialog
    7. Paste the link into a real WhatsApp chat (to yourself works) and into iMessage if available
    8. Verify within 30 seconds the preview renders with: emerald gradient background, "Despedida de Carlos en Cádiz" as the title in white, "Creado por <your name>" below it, the date range in lighter white, and "GroupCoordinator" in the bottom-right corner
    9. Confirm: Lighthouse mobile SEO on /plan/[slug] reports the og:image meta tag is present (open DevTools → Lighthouse → run SEO audit)
    10. Confirm: the share dialog "Compartir vía..." button on iOS Safari (real device, not simulator) opens the native share sheet; on desktop Firefox, only "Copiar link" is visible
    11. Tear down: revert `NEXT_PUBLIC_SITE_URL` to `http://localhost:3000`, stop ngrok

    Failure modes to watch for: blank/transparent OG image (font failed to load); title clipped to one character (line-height bug); white-on-white text (gradient direction wrong); 404 on /api/og/[slug] (route file path or runtime mismatch).
  </how-to-verify>
  <resume-signal>Type "approved" once WhatsApp + iMessage render the gradient + title + creator preview correctly, OR describe issues and how the failure manifested.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated client → Server Action | createPlan / mintInviteToken trust the session; reject if no user |
| Server Action → Postgres | RLS policies enforce ownership for plans/plan_members/invite_tokens INSERTs |
| Public crawler → /api/og/[plan_slug] | OG image is intentionally public; uses service-role; must NOT leak unintended data |
| Browser ↔ navigator.clipboard / navigator.share | User-permission-gated; failures are silent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | createPlan sets owner_id from the request body | mitigate | Server Action ignores any owner_id in formData; reads it from `(await supabase.auth.getUser()).id`; RLS plans_insert_authenticated additionally enforces `(select auth.uid()) = owner_id` (Plan 01-02 policy) |
| T-04-02 | Information Disclosure | /api/og/[plan_slug] exposes plan title + creator name to anyone with the slug | accept | Documented as intentional — this is the marketing surface (D-09, MP-5). Slug is 8-char nanoid (~3.5T combinations, not enumerable). archived_at filter prevents archived plans from rendering an OG image |
| T-04-03 | Information Disclosure | /api/og/[plan_slug] uses service-role → could fetch any user's PII | mitigate | Route file selects ONLY `title, start_date, end_date, owner_id`; resolves creator name via admin.getUserById and uses only `full_name`. No email, no other PII; service-role is constrained to this single function call |
| T-04-04 | Spoofing | /plan/new bypass via missing AUTH-06 enforcement | mitigate | `getRequiredUser` checks user + non-anonymous; both server-action and page-route invoke it; failure path redirects to /auth/sign-in?next= |
| T-04-05 | Tampering | use_count race in mintInviteToken-from-share-dialog flow | mitigate | Atomic SQL increment via Drizzle's `sql\`use_count + 1\`` template — single UPDATE statement, race-safe under concurrent opens. Resolved per RESEARCH §Open Question 2 (atomic increment, NOT read-then-write). |
| T-04-06 | Information Disclosure | Plan slug leaks via Referer header when share dialog Copy-to-clipboard fires | mitigate | strict-origin-when-cross-origin Referrer-Policy from Plan 01-03; navigator.clipboard does NOT emit a Referer; the only risk is the user pasting the URL into a less-secure context, which is acceptable user behavior |

</threat_model>

<verification>
- Owner creates plan with title only → lands on /plan/[slug]?share=1 with share dialog open
- Share dialog displays /i/[token] in font-mono, Copy button, Web Share API CTA (where available)
- Copy button replaces icon with Check for 2s on success
- /api/og/[plan_slug] returns 1200×630 PNG with title + creator + gradient
- Plan header (Surface 4) shows plan title + member stack + owner gear icon when owner
- All E2E + integration tests pass on Chromium desktop + WebKit mobile
- Manual checkpoint (Task 3) confirms WhatsApp/iMessage render the OG preview correctly
</verification>

<success_criteria>
- PLAN-01, PLAN-02, AUTH-06 vertical slices complete and E2E-verified
- Form → action → DB → share dialog → invite link flow completes in under 30 seconds (wedge metric)
- OG preview verified visually in WhatsApp (D-09, MP-5)
- No service-role used in createPlan or mintInviteToken (RLS is the source of truth)
- Manual verification approved
</success_criteria>

<output>
Create `.planning/phases/01-spine-plan-lifecycle/01-04-SUMMARY.md` when done. Include:
- Exact pattern used for plans/plan_members/invite_tokens 3-row insert in createPlan (single Supabase client batched query OR three sequential calls — document the choice)
- Confirmation that no service-role is imported in src/server/actions/plan.ts
- Whether OG fonts were fetched from `geist` npm package OR @vercel/fonts CDN — document the path chosen
- The first-render flicker (if any) when ?share=1 auto-opens the dialog — describe whether it's smooth or noticeable
- Manual checkpoint Task 3 outcome (approved with any caveats)
</output>
