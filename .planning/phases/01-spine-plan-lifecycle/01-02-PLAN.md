---
phase: 01-spine-plan-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - drizzle/schema.ts
  - drizzle/db.ts
  - drizzle/migrations/0001_init.sql
  - drizzle/migrations/meta/_journal.json
  - supabase/migrations/001_auth_hook.sql
  - supabase/migrations/002_rls_enable.sql
  - supabase/policies/plans.sql
  - supabase/policies/plan_members.sql
  - supabase/policies/invite_tokens.sql
  - supabase/seed.sql
  - src/lib/supabase/server.ts
  - src/lib/supabase/browser.ts
  - src/lib/supabase/service-role.ts
  - src/lib/auth/invite-token.ts
  - src/lib/auth/permissions.ts
  - src/lib/db/queries/plans.ts
  - src/lib/validation/plan.ts
  - tests/unit/token-alphabet.test.ts
  - tests/integration/rls-plans.test.ts
  - tests/integration/rls-plan-members.test.ts
  - tests/integration/rls-invite-tokens.test.ts
  - tests/integration/auth-hook.test.ts
autonomous: false
requirements:
  - AUTH-01
  - AUTH-05
truths:
  - "Drizzle schema declares `plans`, `plan_members`, `invite_tokens` exactly per RESEARCH §Area 5 step 7 plus the `(plan_id, user_id)` unique constraint on plan_members (RESEARCH §Open Question 3)"
  - "RLS is ENABLED on all 3 Phase 1 tables — `SELECT count(*) FROM pg_tables WHERE rowsecurity=true AND tablename IN ('plans','plan_members','invite_tokens')` returns 3"
  - "All 10 RLS policies from RESEARCH §Area 4 exist verbatim — anon SELECT via plan_id claim, authenticated member SELECT, owner-only INSERT/UPDATE on plans, etc."
  - "Custom Access Token Hook `public.custom_access_token_hook` is installed and registered in supabase/config.toml (D-18, D-19)"
  - "Invite token generator uses no-lookalike alphabet `23456789abcdefghjkmnpqrstuvwxyz`, 22 chars (128+ bits, HP-6 / D-05); plan slug uses 8-char lowercase-alphanumeric (architecture default)"
  - "Service-role client lives in src/lib/supabase/service-role.ts with persistSession:false; top-of-file comment marks it server-only; no client component imports it (D-18, anti-pattern 1)"
  - "Seed data inserts one test plan + one valid invite token + one seeded test user account (test@groupcoordinator.local) for E2E auth bypass"
  - "RLS isolation: anon-without-claim cannot read any plan; anon-with-correct-claim reads only the matching plan; cross-plan leakage is structurally prevented (MP-3, threat T-02-04)"
must_haves:
  truths:
    - "Database schema is the security spine — every later phase inherits these RLS policies"
    - "Auth hook promotes plan_id from app_metadata (server-only) to top-level JWT claim — pitfall 1 (user_metadata abuse) is structurally prevented"
    - "Drizzle owns DDL; raw SQL owns RLS — pitfall 4 (DROP POLICY clash) is structurally prevented"
    - "User identity is stable via auth.users.id (D-21 MP-3 GDPR pseudonymization)"
  artifacts:
    - path: drizzle/schema.ts
      provides: "TypeScript schema for plans, plan_members, invite_tokens with planMemberRoleEnum"
      contains: "pgEnum, pgTable, plans, plan_members, invite_tokens, unique constraint on (plan_id, user_id)"
    - path: supabase/migrations/001_auth_hook.sql
      provides: "public.custom_access_token_hook function reading app_metadata.plan_id and promoting to top-level claim"
      contains: "create or replace function public.custom_access_token_hook"
    - path: supabase/policies/plans.sql
      provides: "4 policies: plans_select_anon_with_claim, plans_select_member, plans_insert_authenticated, plans_update_owner_only"
      contains: "create policy"
    - path: supabase/policies/plan_members.sql
      provides: "5 policies covering anon SELECT, authenticated SELECT, self-or-owner INSERT, owner-or-self UPDATE, owner-or-self DELETE"
    - path: supabase/policies/invite_tokens.sql
      provides: "3 policies: select_owner, insert_owner, update_owner; no DELETE policy (hard-delete forbidden per RESEARCH §Area 4)"
    - path: src/lib/supabase/server.ts
      provides: "createServerClient(cookieStore) using @supabase/ssr getAll/setAll cookie pattern"
      contains: "createServerClient"
    - path: src/lib/supabase/service-role.ts
      provides: "createServiceRoleClient() — server-only, persistSession false; top comment 'NEVER IMPORT FROM CLIENT'"
      contains: "persistSession: false"
    - path: src/lib/auth/invite-token.ts
      provides: "generateToken() → 22-char nanoid no-lookalike; generateSlug() → 8-char lowercase-alphanumeric"
      contains: "customAlphabet"
    - path: supabase/seed.sql
      provides: "One test owner user (auth.users + identity), one test plan, one valid invite token, one revoked invite token, one expired invite token"
  key_links:
    - from: src/lib/supabase/server.ts
      to: src/middleware.ts
      via: "@supabase/ssr setAll cookie handler writes to NextResponse"
      pattern: "setAll"
    - from: supabase/migrations/001_auth_hook.sql
      to: supabase/config.toml
      via: "pg-functions://postgres/public/custom_access_token_hook URI"
      pattern: "custom_access_token_hook"
    - from: drizzle/schema.ts
      to: supabase/policies/*.sql
      via: "table names match — policies reference Drizzle-generated tables"
      pattern: "plans|plan_members|invite_tokens"
user_setup:
  - service: supabase-auth-hook
    why: "Custom Access Token Hook must be ENABLED in the Supabase Studio Authentication → Hooks UI; CLI registers the URI but the toggle is a one-time human action per environment"
    env_vars: []
    dashboard_config:
      - task: "Open http://localhost:54323 → Authentication → Hooks → Custom Access Token Hook → enable + select public.custom_access_token_hook"
        location: "Supabase Studio (local)"
---

<objective>
Land the database schema, RLS policy spine, Custom Access Token Hook, Supabase client triplet, and invite-token utility functions. After this plan, the database structurally enforces "anon-with-correct-plan_id-claim can read one plan; anon-without-claim can read nothing; authenticated owners can manage their plans; service-role is server-only" — and integration tests prove it.

Purpose: This is the SECURITY SPINE of the entire project. Every later phase queries through these RLS policies. Retrofitting RLS is the most expensive class of refactor in this stack (ROADMAP build-order rationale). Drizzle owns DDL; raw SQL owns RLS — the official Supabase × Drizzle split per RESEARCH §Area 3.

Output: 3 tables + 10+ RLS policies + 1 PL/pgSQL hook + 3 Supabase clients + token utilities + 4 integration test specs proving RLS enforcement.

User story (MVP_MODE — vertical slice progression): **As a** Phase 1 contributor, **I want to** have a tested database spine where RLS structurally enforces plan isolation, **so that** the anonymous invite flow (Plan 01-03) and authenticated plan creation (Plan 01-04) can be built on top without re-litigating security boundaries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-spine-plan-lifecycle/01-CONTEXT.md
@.planning/phases/01-spine-plan-lifecycle/01-RESEARCH.md
@.planning/phases/01-spine-plan-lifecycle/01-VALIDATION.md
@.planning/phases/01-spine-plan-lifecycle/01-SKELETON.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-spine-plan-lifecycle/01-01-SUMMARY.md

<interfaces>
<!-- Contracts produced by this plan that downstream plans consume: -->

src/lib/supabase/server.ts:
  export function createServerClient(cookieStore: ReadonlyRequestCookies): SupabaseClient
  // Used by every server route, server component, server action

src/lib/supabase/browser.ts:
  export function getBrowserClient(): SupabaseClient
  // Used by client components only (sign-in bottom sheet, share dialog Copy button)

src/lib/supabase/service-role.ts:
  export function createServiceRoleClient(): SupabaseClient
  // SERVER-ONLY. Used by /api/invite/[token]/route.ts, /auth/callback/route.ts, migrations, seed scripts

src/lib/auth/invite-token.ts:
  export function generateToken(): string  // 22-char no-lookalike
  export function generateSlug(): string   // 8-char lowercase alphanumeric

src/lib/auth/permissions.ts:
  export function isOwner(plan: { ownerId: string }, userId: string | null): boolean
  export function isMember(memberships: Array<{ planId: string }>, planId: string): boolean

drizzle/schema.ts table shapes:
  plans: id (uuid pk), slug (text unique), title (text), description (text?),
         start_date (timestamptz?), end_date (timestamptz?), owner_id (uuid),
         archived_at (timestamptz?), created_at, updated_at
  plan_members: id, plan_id (uuid fk → plans.id cascade), user_id (uuid),
                role (enum: owner|editor|viewer), joined_via_token_id (uuid?),
                joined_at, UNIQUE(plan_id, user_id)
  invite_tokens: id, plan_id (uuid fk → plans.id cascade), token (text unique),
                 role (enum), expires_at (timestamptz?), revoked_at (timestamptz?),
                 created_by (uuid), use_count (integer default 0), created_at

JWT claim format produced by Custom Access Token Hook:
  { ..., plan_id: "<uuid>", app_metadata: { plan_id: "<uuid>", invite_token_id: "<uuid>" } }
  // Top-level plan_id is what RLS policies read; app_metadata nested copy is the source-of-truth (server-set)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drizzle schema + migrations + Supabase client triplet + token utilities</name>
  <files>drizzle/schema.ts, drizzle/db.ts, drizzle/migrations/0001_init.sql, drizzle/migrations/meta/_journal.json, src/lib/supabase/server.ts, src/lib/supabase/browser.ts, src/lib/supabase/service-role.ts, src/lib/auth/invite-token.ts, src/lib/auth/permissions.ts, src/lib/db/queries/plans.ts, src/lib/validation/plan.ts</files>
  <read_first>
    - .planning/phases/01-spine-plan-lifecycle/01-RESEARCH.md §Area 3 (Drizzle/Supabase split, config.ts, db.ts), §Area 5 step 7 (schema.ts exact shape) + step 10 (Supabase client triplet code) + §Open Question 3 (unique constraint requirement)
    - .planning/phases/01-spine-plan-lifecycle/01-CONTEXT.md (D-05 nanoid alphabet, D-15 Drizzle, D-21 stable user_id, D-18/19 RLS rules)
    - .planning/research/ARCHITECTURE.md §Data Model (entity shapes), §Routing Structure §Token Generation & Revocation (alphabet + entropy)
    - .planning/phases/01-spine-plan-lifecycle/01-SKELETON.md (architectural decisions — schema/RLS split must hold)
  </read_first>
  <action>
    1) Write `drizzle/schema.ts` exactly per RESEARCH §Area 5 step 7 with these explicit additions/changes:
       - Add `export const planMemberRoleEnum = pgEnum('plan_member_role', ['owner', 'editor', 'viewer'])` at top.
       - On `planMembers` add `unique('plan_members_plan_user_unique').on(table.planId, table.userId)` (RESEARCH §Open Question 3 — required for the OAuth callback upsert in Plan 01-05).
       - On `plans` add `archivedAt: timestamp('archived_at', { withTimezone: true })` (D-05 soft-delete; PLAN-05 needs it).
       - On `inviteTokens` add `useCount: integer('use_count').notNull().default(0)` (UI-SPEC Surface 6 token row meta — RESEARCH §Open Question 2: use atomic SQL increment, NOT read-then-write).
       - Add `updatedAt` to `plans` with default `now()` and a trigger note (the auto-update trigger is added in the migration SQL below; Drizzle doesn't emit triggers from schema.ts).
       - All `owner_id`, `user_id`, `created_by` columns are `uuid` typed; cross-schema FKs to `auth.users(id)` are NOT declared in Drizzle (Postgres allows it but Drizzle introspection breaks across schemas — RESEARCH §Area 5 step 7 note).

    2) Generate the migration: run `pnpm drizzle-kit generate`. Verify the generated SQL in `drizzle/migrations/0001_init.sql` creates the enum, all three tables, the unique constraint on plan_members, and references plans.id with `ON DELETE CASCADE` on plan_members and invite_tokens.

    3) Write `drizzle/db.ts` per RESEARCH §Area 3: import drizzle from `drizzle-orm/postgres-js`, `postgres` driver, schema from `./schema`. Use `process.env.DATABASE_URL!` (runtime — port 6543 in production, 54322 local). Export named `db`.

    4) Write the three Supabase clients (RESEARCH §Area 5 step 10):
       - `src/lib/supabase/server.ts` — `createServerClient(cookieStore: ReadonlyRequestCookies)` using `@supabase/ssr` with `getAll` + `setAll` cookie handlers. Wrap `setAll` in try/catch (Server Components throw if you try to set cookies during render).
       - `src/lib/supabase/browser.ts` — singleton `getBrowserClient()` using `@supabase/ssr`'s `createBrowserClient`.
       - `src/lib/supabase/service-role.ts` — top-of-file comment block: `// SERVER-ONLY. NEVER IMPORT FROM CLIENT COMPONENTS. // RLS bypass — used only in /api/invite/[token], /auth/callback, migrations, seed scripts.` Exports `createServiceRoleClient()` with `auth: { persistSession: false, autoRefreshToken: false, detectSessionInUrl: false }`. Add a runtime guard: `if (typeof window !== 'undefined') throw new Error('service-role client must not be imported in browser bundles')`.

    5) Write `src/lib/auth/invite-token.ts`:
       - `const SLUG_ALPHABET = '0123456789abcdefghijklmnopqrstuvwxyz'` and `const TOKEN_ALPHABET = '23456789abcdefghjkmnpqrstuvwxyz'`.
       - `export const generateSlug = customAlphabet(SLUG_ALPHABET, 8)`.
       - `export const generateToken = customAlphabet(TOKEN_ALPHABET, 22)`.
       - Export the alphabets as `_TOKEN_ALPHABET` / `_SLUG_ALPHABET` for tests.

    6) Write `src/lib/auth/permissions.ts` with `isOwner(plan, userId)` and `isMember(memberships, planId)` pure functions — easy to unit-test, no DB calls. Used by RSC pages to decide which UI affordances to render (RLS is still the SOURCE OF TRUTH; these helpers are presentational gating only — comment this explicitly).

    7) Write `src/lib/db/queries/plans.ts` with three query helpers that take a Drizzle DB client + a Supabase client (for RLS-bound queries, executors should prefer the Supabase client; Drizzle is for service-role/seed contexts). Helpers:
       - `getPlanBySlug(supabase, slug)` — returns the plan row (RLS-filtered to caller's permissions).
       - `getMyPlans(supabase, userId)` — returns plans where userId is owner OR a plan_members entry exists (RLS filters automatically; the function still scopes by `owner_id OR auth.uid()` for clarity).
       - `getPlanMembers(supabase, planId)` — returns plan_members joined with auth.users for display name resolution at render (D-21 — no denormalized display_name column).

    8) Write `src/lib/validation/plan.ts` with `createPlanSchema` (Zod) per RESEARCH §Area 5 step 12: title min(1) max(200), startDate/endDate optional ISO strings, description optional max(1000). Add `archivePlanSchema` (planId uuid only). Add `revokeTokenSchema` (tokenId uuid only). All schemas exported by name; shared client + server (D-15 / STACK.md pattern).

    9) Update root `package.json` to ensure these scripts: `db:generate` already present from Plan 01-01; add `db:reset` → `supabase db reset` (drops local DB + reapplies migrations + reseeds — used by integration test setup).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm drizzle-kit generate --dry-run 2>&1 | tail -5 && grep -c "create policy" supabase/policies/*.sql 2>/dev/null; ls drizzle/migrations/*.sql | head -1 && grep -E 'plan_members_plan_user_unique|UNIQUE.*plan_id.*user_id' drizzle/migrations/0001_init.sql && grep -E 'persistSession: false' src/lib/supabase/service-role.ts && grep -E 'customAlphabet\(.*23456789abcdefghjkmnpqrstuvwxyz.*, 22\)' src/lib/auth/invite-token.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit` exits 0
    - `drizzle/migrations/0001_init.sql` contains `CREATE TYPE` for `plan_member_role` AND `CREATE TABLE` for plans, plan_members, invite_tokens
    - The migration file contains `UNIQUE` constraint on `(plan_id, user_id)` for plan_members (grep matches `plan_members_plan_user_unique` OR `UNIQUE.*plan_id.*user_id`)
    - The migration file contains `ON DELETE CASCADE` on plan_members.plan_id FK and invite_tokens.plan_id FK (grep `ON DELETE CASCADE` ≥ 2 hits)
    - `grep -E "archived_at" drizzle/migrations/0001_init.sql` matches (D-05 soft-delete column)
    - `grep -E "use_count.*integer" drizzle/migrations/0001_init.sql` matches
    - `grep -c "if (typeof window !== 'undefined')" src/lib/supabase/service-role.ts` returns 1 (runtime guard)
    - `grep -E "SERVER-ONLY" src/lib/supabase/service-role.ts` matches in top comment
    - `cat src/lib/auth/invite-token.ts | grep -cE "customAlphabet"` returns ≥ 2 (slug + token)
    - `node -e "const {generateToken,generateSlug}=require('./src/lib/auth/invite-token.ts')"` fails (.ts) but `pnpm exec tsx -e "..."` succeeds — OR equivalent runtime smoke via unit test
    - `grep -E "createPlanSchema|archivePlanSchema|revokeTokenSchema" src/lib/validation/plan.ts | wc -l` ≥ 3
    - `grep -E "getPlanBySlug|getMyPlans|getPlanMembers" src/lib/db/queries/plans.ts | wc -l` ≥ 3
  </acceptance_criteria>
  <done>Schema generated, migrations file exists with all DDL constraints, Supabase client triplet present with runtime guards, token utilities use the exact no-lookalike alphabets, Zod schemas declared.</done>
</task>

<task type="auto">
  <name>Task 2: RLS policies + Custom Access Token Hook + seed data</name>
  <files>supabase/migrations/001_auth_hook.sql, supabase/migrations/002_rls_enable.sql, supabase/policies/plans.sql, supabase/policies/plan_members.sql, supabase/policies/invite_tokens.sql, supabase/seed.sql</files>
  <read_first>
    - .planning/phases/01-spine-plan-lifecycle/01-RESEARCH.md §Area 1 (Auth hook SQL exact text), §Area 4 (ALL RLS policy SQL — copy verbatim), §Area 5 step 8 (apply order)
    - .planning/phases/01-spine-plan-lifecycle/01-CONTEXT.md (D-18 RLS mandatory, D-19 anon SELECT via claim, D-21 stable user_id, D-22 token security)
    - .planning/research/PITFALLS.md §HP-6 (token security mitigations), §MP-3 (GDPR pseudonymization — keys identity off auth.users.id)
  </read_first>
  <action>
    1) Write `supabase/migrations/001_auth_hook.sql` — copy the PL/pgSQL function from RESEARCH §Area 1 EXACTLY. The function:
       - Reads `event -> 'claims' -> 'app_metadata' ->> 'plan_id'`
       - If non-null, sets `claims = jsonb_set(claims, '{plan_id}', to_jsonb(plan_id))`
       - Returns `jsonb_build_object('claims', claims)`
       - `grant execute on function public.custom_access_token_hook to supabase_auth_admin`
       - `revoke execute on function public.custom_access_token_hook from public`

    2) Write `supabase/migrations/002_rls_enable.sql` (RESEARCH §Area 4 prelude):
       ```
       alter table plans enable row level security;
       alter table plan_members enable row level security;
       alter table invite_tokens enable row level security;
       grant select, insert, update, delete on plans to authenticated;
       grant select on plans to anon;
       grant select, insert, update, delete on plan_members to authenticated;
       grant select on plan_members to anon;
       grant select, insert, update, delete on invite_tokens to authenticated;
       -- NOTE: invite_tokens deliberately NOT granted to anon (D-22, threat T-02-02)
       ```
       Also add an `updated_at` auto-update trigger function and apply it to `plans`:
       ```
       create or replace function set_updated_at() returns trigger as $$
       begin new.updated_at = now(); return new; end;
       $$ language plpgsql;
       create trigger plans_set_updated_at before update on plans
         for each row execute function set_updated_at();
       ```

    3) Write `supabase/policies/plans.sql` — copy the 4 policies from RESEARCH §Area 4 §plans verbatim (plans_select_anon_with_claim, plans_select_member, plans_insert_authenticated, plans_update_owner_only). DO NOT add a DELETE policy (D-05 + RESEARCH §Open Question 5 — hard-delete forbidden; "Eliminar plan" UI sets archived_at).

    4) Write `supabase/policies/plan_members.sql` — copy the 5 policies from RESEARCH §Area 4 §plan_members verbatim (anon SELECT, authenticated SELECT, INSERT self-or-owner, UPDATE owner-or-self, DELETE owner-or-self).

    5) Write `supabase/policies/invite_tokens.sql` — copy the 3 policies from RESEARCH §Area 4 §invite_tokens verbatim (SELECT owner, INSERT owner, UPDATE owner). No DELETE policy (audit trail).

    6) Write `supabase/seed.sql` — must run AFTER migrations and policies. Inserts as service-role (bypasses RLS implicitly via direct psql connection):
       - One auth.users row for test owner: id `00000000-0000-0000-0000-000000000099`, email `test@groupcoordinator.local`, encrypted_password = bcrypt hash of `test-password-do-not-use-in-prod`, email_confirmed_at = now(), `app_metadata = '{"provider":"email","providers":["email"]}'::jsonb`, `user_metadata = '{"full_name":"Test Owner"}'::jsonb`, `aud = 'authenticated'`, `role = 'authenticated'`. Also insert into `auth.identities` for this user (provider 'email', identity_data with email + sub matching user id).
       - One auth.users row for a second test user `00000000-0000-0000-0000-000000000088` (used in cross-RLS isolation tests).
       - One plan row: id `00000000-0000-0000-0000-000000000001`, slug `seed-plan`, title `Plan de prueba (seed)`, owner_id matches the test owner, created_at = now() - interval '1 day'.
       - One valid invite token: token `seedvalidtoken22charsX1` (exactly 22 chars, all in no-lookalike alphabet), plan_id matches seed plan, role = 'viewer', created_by = test owner.
       - One revoked invite token: token `seedrevokedtoken22chr22`, revoked_at = now() - interval '1 hour'.
       - One expired invite token: token `seedexpiredtoken22chr11`, expires_at = now() - interval '1 day'.
       - One plan_members row: user_id = `00000000-0000-0000-0000-000000000099`, plan_id = seed plan, role = 'owner', joined_at = now() - interval '1 day'.

       Wrap all seed inserts in `BEGIN; ... COMMIT;` and add `ON CONFLICT (id) DO NOTHING` for idempotency.

    7) Apply everything to local Supabase:
       ```
       pnpm supabase start  # if not already running
       pnpm drizzle-kit migrate
       psql "$DATABASE_MIGRATION_URL" -f supabase/migrations/001_auth_hook.sql
       psql "$DATABASE_MIGRATION_URL" -f supabase/migrations/002_rls_enable.sql
       psql "$DATABASE_MIGRATION_URL" -f supabase/policies/plans.sql
       psql "$DATABASE_MIGRATION_URL" -f supabase/policies/plan_members.sql
       psql "$DATABASE_MIGRATION_URL" -f supabase/policies/invite_tokens.sql
       psql "$DATABASE_MIGRATION_URL" -f supabase/seed.sql
       ```

    8) DO NOT enable the Custom Access Token Hook via SQL — that toggle lives in `supabase/config.toml` (set in Plan 01-01) plus a Studio UI toggle that the developer flips once per environment (covered in `user_setup`).
  </action>
  <verify>
    <automated>psql "$DATABASE_MIGRATION_URL" -c "SELECT count(*) FROM pg_tables WHERE rowsecurity=true AND tablename IN ('plans','plan_members','invite_tokens');" -t | tr -d ' ' | grep -E '^3$' && psql "$DATABASE_MIGRATION_URL" -c "SELECT count(*) FROM pg_policies WHERE schemaname='public' AND tablename IN ('plans','plan_members','invite_tokens');" -t | tr -d ' ' | awk '$1>=10' && psql "$DATABASE_MIGRATION_URL" -c "SELECT proname FROM pg_proc WHERE proname='custom_access_token_hook';" -t | grep -c custom_access_token_hook && psql "$DATABASE_MIGRATION_URL" -c "SELECT count(*) FROM plans WHERE slug='seed-plan';" -t | tr -d ' ' | grep -E '^1$' && psql "$DATABASE_MIGRATION_URL" -c "SELECT count(*) FROM invite_tokens WHERE plan_id='00000000-0000-0000-0000-000000000001';" -t | tr -d ' ' | grep -E '^3$'</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT count(*) FROM pg_tables WHERE rowsecurity=true AND tablename IN ('plans','plan_members','invite_tokens')` returns 3
    - `SELECT count(*) FROM pg_policies WHERE schemaname='public' AND tablename IN ('plans','plan_members','invite_tokens')` returns ≥ 10 (4 + 5 + 3 minimum)
    - `SELECT count(*) FROM pg_policies WHERE tablename='plans' AND policyname IN ('plans_select_anon_with_claim','plans_select_member','plans_insert_authenticated','plans_update_owner_only')` returns 4
    - `SELECT proname FROM pg_proc WHERE proname='custom_access_token_hook'` returns one row
    - `SELECT has_function_privilege('supabase_auth_admin', 'public.custom_access_token_hook(jsonb)', 'execute')` returns `t`
    - Seed: `SELECT count(*) FROM plans WHERE slug='seed-plan'` returns 1
    - Seed: `SELECT count(*) FROM invite_tokens WHERE plan_id='00000000-0000-0000-0000-000000000001'` returns 3 (valid + revoked + expired)
    - Seed: `SELECT count(*) FROM auth.users WHERE email='test@groupcoordinator.local'` returns 1
    - `grep -c "create policy" supabase/policies/*.sql | awk -F: '{sum+=$2} END {print sum}'` returns ≥ 12 (one per policy + idempotent CREATE POLICY IF NOT EXISTS if used — RESEARCH used plain CREATE POLICY)
    - No DELETE policy on plans table: `psql -c "SELECT count(*) FROM pg_policies WHERE tablename='plans' AND cmd='DELETE'"` returns 0
    - No DELETE or SELECT policy granting anon on invite_tokens: `psql -c "SELECT count(*) FROM pg_policies WHERE tablename='invite_tokens' AND 'anon' = ANY(roles)"` returns 0
  </acceptance_criteria>
  <done>RLS enforced on all 3 tables; 10+ policies installed verbatim from RESEARCH §Area 4; auth hook function compiled and granted to supabase_auth_admin; seed data inserted (one plan, three tokens, two test users); manual Studio toggle for the hook still required (in user_setup).</done>
</task>

<task type="auto">
  <name>Task 3: RLS isolation + auth hook integration tests</name>
  <files>tests/unit/token-alphabet.test.ts, tests/integration/rls-plans.test.ts, tests/integration/rls-plan-members.test.ts, tests/integration/rls-invite-tokens.test.ts, tests/integration/auth-hook.test.ts</files>
  <read_first>
    - .planning/phases/01-spine-plan-lifecycle/01-RESEARCH.md §Validation Architecture §RLS Policy Tests (test scaffold), §Area 4 (policy expectations)
    - .planning/phases/01-spine-plan-lifecycle/01-VALIDATION.md §Critical Paths (RLS isolation is critical path 6)
    - supabase/policies/*.sql (created Task 2; tests assert these policies enforce what they declare)
    - tests/setup/supabase.ts (Plan 01-01 — service-role test client helper)
  </read_first>
  <action>
    1) `tests/unit/token-alphabet.test.ts` — tests the exported `_TOKEN_ALPHABET` and `_SLUG_ALPHABET` constants. Asserts: token alphabet has length 32; slug alphabet has length 36; neither contains `0`, `1`, `I`, `l`, `O` for the token alphabet; slug alphabet is lowercase-alphanumeric. Asserts 1000 generated tokens contain no characters outside the alphabet (regex check `/^[23456789abcdefghjkmnpqrstuvwxyz]+$/`).

    2) `tests/integration/rls-plans.test.ts` — uses `tests/setup/supabase.ts` to mint Supabase clients in 4 personas:
       - Anonymous WITHOUT plan_id claim (default anon client)
       - Anonymous WITH plan_id claim matching the seed plan (mint via service-role: create anonymous user, set app_metadata.plan_id = seed plan id, refresh session, hand the access_token to a new client)
       - Anonymous WITH plan_id claim matching a DIFFERENT (non-existent) uuid
       - Authenticated owner (signInWithPassword as test@groupcoordinator.local)

       Assertions:
       - Anon-no-claim: `from('plans').select('*')` returns 0 rows
       - Anon-correct-claim: returns exactly 1 row, the seed plan
       - Anon-wrong-claim: returns 0 rows
       - Owner: `from('plans').select('*').eq('owner_id', ownerId)` returns ≥ 1 row
       - Owner inserting a new plan: succeeds (becomes owner)
       - Authenticated non-owner trying to UPDATE seed plan: returns 0 rows updated (RLS USING + WITH CHECK both filter)

    3) `tests/integration/rls-plan-members.test.ts`:
       - Anon-with-correct-plan_id-claim can SELECT plan_members for the seed plan (returns the owner's row)
       - Anon-with-wrong-claim sees 0 plan_members rows
       - A new authenticated user (different from owner) cannot INSERT a plan_members row for themselves on the seed plan WITHOUT app_metadata (because the policy USES the (select auth.uid()) match — but wait: the policy allows `user_id = (select auth.uid())`, so this WOULD succeed; THIS IS BY DESIGN per RESEARCH §Area 4 — the OAuth callback inserts the plan_members row using the user's own auth.uid()). Adjust this test to assert: authenticated user CAN insert their own plan_members row but CANNOT insert one with a different user_id, AND cannot UPDATE another member's row.

    4) `tests/integration/rls-invite-tokens.test.ts`:
       - Anonymous client (with OR without plan_id claim) cannot read ANY invite_tokens row (returns 0) — proves the `revoke select on invite_tokens from anon` decision from Task 2
       - Authenticated owner can SELECT, INSERT, UPDATE invite_tokens for plans they own
       - Authenticated non-owner cannot SELECT or UPDATE invite_tokens for plans they don't own
       - Direct DELETE attempt by owner returns 0 rows affected (no DELETE policy)

    5) `tests/integration/auth-hook.test.ts`:
       - Uses service-role client to create a new anonymous user, set `app_metadata.plan_id = <seed plan id>` via `auth.admin.updateUserById`, then mint a new session for that user (using `auth.admin.generateLink({ type: 'magiclink' })` and parsing the access_token, OR using a more direct approach: read the new user's app_metadata, then use the user's id to mint a Supabase JWT manually via the JWT secret — RESEARCH §Area 1 actually says: call `refreshSession` from a server client after `updateUserById`; encode this via the Supabase Admin API `admin.signOut` + new `signInAnonymously` approach).
       - The simplest robust path: spin up a Postgres connection, call `select public.custom_access_token_hook('{"claims":{"sub":"00000000-0000-0000-0000-000000000088","app_metadata":{"plan_id":"00000000-0000-0000-0000-000000000001"}}}'::jsonb)` directly via SQL. Assert the returned `claims` JSON contains a top-level `plan_id` matching the seed plan id. This proves the hook function does what RESEARCH §Area 1 claims, independent of Supabase Auth's actual hook invocation flow.
       - Second assertion: same call with no `app_metadata.plan_id` returns claims WITHOUT a top-level plan_id key.

    6) Configure these integration tests to require local Supabase up. Add a top-level `beforeAll` that pings `process.env.DATABASE_MIGRATION_URL` and `skip`s the suite if unreachable, with a clear "run `pnpm supabase start` first" message.
  </action>
  <verify>
    <automated>pnpm supabase start 2>&1 | tail -2; pnpm test:unit --run tests/unit/token-alphabet.test.ts && pnpm test:unit --run tests/integration/ 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:unit --run tests/unit/token-alphabet.test.ts` exits 0
    - `pnpm test:unit --run tests/integration/rls-plans.test.ts` exits 0 with ≥ 6 assertions passing (the 6 plan personas + owner update path)
    - `pnpm test:unit --run tests/integration/rls-plan-members.test.ts` exits 0 with ≥ 4 assertions
    - `pnpm test:unit --run tests/integration/rls-invite-tokens.test.ts` exits 0 with ≥ 4 assertions (anon blocked, owner allowed, non-owner blocked, delete blocked)
    - `pnpm test:unit --run tests/integration/auth-hook.test.ts` exits 0 with ≥ 2 assertions (claim promoted; no app_metadata → no top-level plan_id)
    - All integration tests skip gracefully (not error) if local Supabase is not running — `grep -E "skip|Supabase not running" tests/integration/*.test.ts | wc -l` ≥ 4
  </acceptance_criteria>
  <done>RLS isolation proven by 4 integration test files; auth hook function tested at the SQL level; cross-plan leakage structurally impossible. VALIDATION.md per-task verification map updated with RLS-* test rows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Enable Custom Access Token Hook in Supabase Studio (one-time manual toggle)</name>
  <files>(none — manual verification task; no file modifications)</files>
  <action>Open Supabase Studio and enable the Custom Access Token Hook for public.custom_access_token_hook (no Claude-automated step).</action>
  <verify>Smoke command `pnpm exec tsx -e ...` printed in <how-to-verify> step 7 returns a JWT containing plan_id claim.</verify>
  <done>Hook toggle ON and verified via JWT payload inspection.</done>
  <what-built>
    The auth hook function (`public.custom_access_token_hook`) is installed and tested at the SQL level. But Supabase Auth only INVOKES the hook if the toggle is enabled in Studio for the local instance. This is the one Phase 1 step that genuinely cannot be automated — Studio's hook UI does not have a CLI/API equivalent in the 2.x release line. Once flipped, the toggle persists in the local Postgres instance.
  </what-built>
  <how-to-verify>
    1. Confirm Supabase local is running: `pnpm supabase status` shows the API URL and Studio URL
    2. Open Supabase Studio at `http://localhost:54323`
    3. Navigate: Authentication → Hooks
    4. Find "Custom Access Token Hook" — toggle ENABLED
    5. In the function selector, choose `public.custom_access_token_hook`
    6. Save the hook configuration
    7. Smoke-test from a terminal: call the integration auth-hook test path with a real anonymous sign-in. Run: `pnpm exec tsx -e "const {createClient}=require('@supabase/supabase-js');const sb=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY);(async()=>{const r=await sb.auth.signInAnonymously();const u=r.data.user.id;const adm=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY,{auth:{persistSession:false}});await adm.auth.admin.updateUserById(u,{app_metadata:{plan_id:'00000000-0000-0000-0000-000000000001'}});const ref=await sb.auth.refreshSession();const tok=ref.data.session.access_token;const payload=JSON.parse(Buffer.from(tok.split('.')[1],'base64url').toString());console.log('plan_id claim:',payload.plan_id);})()"`
    8. Confirm the output prints `plan_id claim: 00000000-0000-0000-0000-000000000001`
    9. If the claim is missing or empty, the hook toggle is OFF or wired to the wrong function — fix in Studio and re-run

    Note: the same toggle must be re-done in the hosted Supabase project (staging/prod) when those environments come online. That's tracked in Phase 7 launch checklist, not Phase 1.
  </how-to-verify>
  <resume-signal>Type "approved" once the JWT contains the top-level `plan_id` claim after the smoke test, or describe the failure mode.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous client → Postgres (via Supabase Auth) | RLS policies are the only gate; anon role must have minimal grants |
| Authenticated client → Postgres | RLS policies enforce ownership + membership; auth.uid() is trusted |
| Server route → Supabase Admin API | Service-role client bypasses RLS; must be quarantined to server-only files |
| JWT issuer (Supabase Auth) → app | Custom Access Token Hook injects plan_id from app_metadata (server-controlled) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Anonymous JWT plan_id claim | mitigate | Claim sourced from `app_metadata` (server-set via service-role only); user_metadata path explicitly avoided per RESEARCH §Area 1 Pitfall 1 |
| T-02-02 | Information Disclosure | invite_tokens enumeration via anon SELECT | mitigate | `revoke select on invite_tokens from anon` in Task 2; only authenticated owner can SELECT (policy invite_tokens_select_owner) |
| T-02-03 | Elevation of Privilege | Anon trying to INSERT/UPDATE plans or plan_members | mitigate | No INSERT/UPDATE/DELETE grant to anon role; even if claim is forged, role-level grant blocks writes before policy evaluation |
| T-02-04 | Information Disclosure | Cross-plan leakage (User A reads User B's plan) | mitigate | RLS plans_select_anon_with_claim requires `id = (auth.jwt() ->> 'plan_id')::uuid`; cast returns NULL if claim missing/wrong; tests in Task 3 prove isolation |
| T-02-05 | Elevation of Privilege | service-role client imported into a browser bundle | mitigate | Top-of-file SERVER-ONLY comment + runtime `if (typeof window !== 'undefined') throw` guard in service-role.ts; Anti-Pattern 1 from security_threat_model |
| T-02-06 | Tampering | Drizzle regenerates migration and accidentally drops RLS policies | mitigate | RLS lives in `supabase/policies/*.sql` ONLY; Drizzle schema.ts never references CREATE POLICY (RESEARCH §Area 3 + Pitfall 4); biome ignore drizzle/migrations |
| T-02-07 | Repudiation | Plan deletion erases audit trail | mitigate | No DELETE policy on plans or invite_tokens — only `archived_at` / `revoked_at` soft-delete (D-05, RESEARCH §Open Question 5) |
| T-02-08 | Information Disclosure | GDPR right-to-erasure with denormalized display_name | mitigate | D-21 + MP-3: display_name resolved at render from auth.users.user_metadata.full_name; no denormalized copies in plans or plan_members |

</threat_model>

<verification>
- All 3 tables have rowsecurity=true; all 10+ policies installed
- Auth hook function present, executable by supabase_auth_admin, executable from SQL
- Seed data lands: 1 plan, 3 tokens (valid/revoked/expired), 2 test users
- Integration tests in Task 3 exit 0 (or skip cleanly when Supabase not running)
- Custom Access Token Hook flipped ON in Studio (Task 4 checkpoint) and verified producing the top-level plan_id JWT claim
</verification>

<success_criteria>
- Drizzle schema generates valid migration; unique (plan_id, user_id) constraint present
- 10+ RLS policies installed exactly as RESEARCH §Area 4 specifies
- Custom Access Token Hook compiled, granted, and functionally injects plan_id at token issuance (Task 4 confirmation)
- Service-role client quarantined behind runtime guard + comment + dedicated file
- Token utilities use no-lookalike 22-char alphabet (HP-6 / D-05)
- 4 integration test files cover plans/plan_members/invite_tokens/auth-hook isolation
- Seed data supports Plan 01-03 (anon link view) and Plan 01-05 (OAuth bypass for tests)
</success_criteria>

<output>
Create `.planning/phases/01-spine-plan-lifecycle/01-02-SUMMARY.md` when done. Include:
- Names of all 10+ policies installed (output of `SELECT policyname FROM pg_policies WHERE schemaname='public'`)
- Confirmation that no DELETE policy exists on `plans` or `invite_tokens`
- Confirmation that the Studio hook toggle was flipped (Task 4 approval)
- Seed UUIDs and tokens (the 22-char test tokens) for Plans 01-03 and 01-05 to reference
- Any deviation from RESEARCH §Area 4 policy SQL and why
</output>
